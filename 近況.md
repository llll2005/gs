此文主要給claude確認這段時間做了哪些實驗 若有漏再讓claude自己讀conf claude也可以自己分析結果


各項實驗：
w = =-0.5的實驗結束 皆負結果
speed3_ultra 成功做完 但參數設定使得其只有0.77M顆點 失敗
其他speed3變體 都OOM

--- 

清除殼實驗：
這是一套專為 **1500 步極速探針（Fast Probe）** 設計的實驗方案與完整 Config。

這套實驗的目的，是在 **1500 步內（約 3~5 分鐘）** 迅速驗證「如何打破 Step 0 偽深度誤差 + Step 1 透射率鎖死」所造成的浮空厚殼。1500 步剛好涵蓋了 **Step 0 初始化**、**Step 1 全相機 `start_trim` 剪枝**，以及 **Step 1000/1100/1250/1400 前幾輪 MCMC 的點數處決與 Relocate 搬家**。

---

### 一、 1500 步「清殼探針」四臂對照實驗設計

我們透過**「改變輸入源（Input）」**、**「解鎖 Step 1 剪枝鎖死（Parameter）」**與**「動態幾何重分配（Algorithm）」**三個維度進行對照：

| 實驗組別 | 測試假說與目的 | 執行指令與關鍵覆蓋 | 評測工具重點 |
| :--- | :--- | :--- | :--- |
| **Arm A (基準對照)** | 現行 `speed3` 配方 + 標準 `depth-init` PLY (初始 \\(o=0.99\\))，記錄 1500 步時的原始厚殼作為對照。 | `python main.py fit --config configs/probe_shell_1500.yaml --model.initialize_from data/matrix_city/aerial/train/block_all/depth_init/block_12.ply --data.parser.block_id 12 -n probe_armA_baseline` | 記錄 z-軸中位數與 Floater % |
| **Arm B (SfM 種子源頭切斷)** | **[Input 改造]** 完全繞過單目偽深度，改用 COLMAP SfM 稀疏點雲（0% 厚殼，點位 100% 貼近真表面）。 | `python main.py fit --config configs/probe_shell_1500.yaml --model.initialize_from null --data.parser.block_id 12 -n probe_armB_sfm` | 驗證從 0 厚殼出發在 1500 步的長點動態 |
| **Arm C (低初始不透明度)** | **[Parameter 改造]** 將初始不透明度降至 \\(o=0.05\\)。讓透射率 \\(T\\) 穿透多層，**防止 Step 1 `start_trim` 一刀切除後方真表面**。 | `python main.py fit --config configs/probe_shell_1500.yaml --model.initialize_from data/matrix_city/aerial/train/block_all/depth_init_o05/block_12.ply --data.parser.block_id 12 -n probe_armC_low_opacity` | 觀察 Step 1 剪枝後後層真表面的存活率 |
| **Arm D (AbsGS 2.0 早期重分配)** | **[Algorithm 改造]** 提早至 100 步開啟 MCMC 增生，並利用 AbsGS 2.0 (\\(|g|\\)) 強制將 Step 1000 清除的死點分配至高殘差真表面。 | `python main.py fit --config configs/probe_shell_1500.yaml --model.initialize_from data/matrix_city/aerial/train/block_all/depth_init/block_12.ply --data.parser.block_id 12 --model.density.init_args.densify_from_iter 100 -n probe_armD_absgrad_early` | 量測 MCMC 是否順利將前層質量轉移至真表面 |

---

### 二、 1500 步探針完整 YAML 配置文件 (`configs/probe_shell_1500.yaml`)

請將以下內容儲存為 `configs/probe_shell_1500.yaml`。此 YAML 已對齊 `speed3_ultra` 的無損加速算子（含 `EXACT_SUPPORT`、`skip_surf_normal` 與 `fast_noise`），並精確鎖定於 **1500 步結束並儲存 Checkpoint**：

```yaml
model:
  initialize_from: null          # 由 CLI 動態覆蓋 (--model.initialize_from)
  overwrite_config: False
  gaussian:
    class_path: internal.models.gaussian_2d.Gaussian2D
    init_args:
      sh_degree: 3              # 保留 SH3 色彩
      optimization:
        means_lr_init: 0.000064
        means_lr_scheduler:
          lr_final: 0.00000064
          max_steps: 1500        # 對齊 1500 步探針
        scales_lr: 0.004
  metric:
    class_path: internal.metrics.mcmc_citygsv2_metrics.MCMCCityGSV2Metrics
    init_args:
      lambda_normal: 0.0         # 關閉法線損失 (觸發零權重門檻)
      normal_regularization_from_iter: 0
      depth_loss_type: l1+ssim
      depth_loss_ssim_weight: 1.0
      depth_loss_weight:
        init: 0.0                # 關閉深度損失 (觸發零權重門檻)
        final_factor: 0.05
        max_steps: 1500
      fused_ssim: false          # 嚴禁開啟 (避開離群落差)
      opacity_reg: 0.002         # 溫和正則
      scale_reg: 0.007
      immune_opacity_threshold: 0.9
  renderer:
    class_path: internal.renderers.sep_depth_trim_2dgs_renderer.SepDepthTrim2DGSRenderer
    init_args:
      depth_ratio: 1.0
      screen_size_prune_px: 300  # 安全尺寸剪枝
      skip_surf_normal: true     # 無損跳過 CUDA 叉積
      dynamic_strips: true       # 動態 K 條帶
      strip_max: 8
      strip_vram_target_gb: 5.2  # 釋放 VRAM 容量至 5.2G
      strip_safety: 0.85
  density:
    class_path: internal.density_controllers.mcmc_2dgs_density_controller.MCMC2DGSDensityController
    init_args:
      cap_max: 2600000           # 容量標定
      absgrad_densify: 2.0       # AbsGS 2.0 (|g| 宿主抽樣)
      fast_noise: true           # 2D 切平面加噪代數簡化
      noise_gate_eps: 0.001      # Noise Gate
      densify_blind_report: false
      noise_lr: 500000.0
      densify_from_iter: 1000    # Arm D 會在 CLI 覆蓋改為 100
      densify_until_iter: 1500   # 探針截至 1500 步
      densification_interval: 150
      min_opacity: 0.005
      N_max: 51
trainer:
  check_val_every_n_epoch: 10
  max_steps: 1500                # 1500 步即刻完賽
data:
  path: data/matrix_city/aerial/train/block_all
  parser:
    class_path: EstimatedDepthBlockColmap
    init_args:
      image_dir: input
      down_sample_factor: 1.2
      content_threshold: 0.08
      min_images_per_block: 10
      block_dim:
      - 5
      - 5
  num_workers: 8
save_iterations:
- 1500                           # 確保存檔 step=1500.ckpt
```

---

### 三、 1500 步評測與幾何診斷指令

當四組探針（Arm A ~ D）在 3~5 分鐘內跑完並在 `outputs/<run_name>/checkpoints/epoch=*-step=1500.ckpt` 生成檔案後，請執行以下幾何審計指令來對比四組的「清殼效果」：

```bash
# 逐一檢查 Step 1500 Checkpoint 的幾何健康度與厚殼指標
python tools/audit_geometry.py outputs/probe_armA_baseline/checkpoints/epoch=*-step=1500.ckpt
python tools/audit_geometry.py outputs/probe_armB_sfm/checkpoints/epoch=*-step=1500.ckpt
python tools/audit_geometry.py outputs/probe_armC_low_opacity/checkpoints/epoch=*-step=1500.ckpt
python tools/audit_geometry.py outputs/probe_armD_absgrad_early/checkpoints/epoch=*-step=1500.ckpt
```

**判讀指標**：
1. **`z-median`（z 軸中位數）**：越接近 **0.26** 代表幾何越貼近真實 SfM 表面；若降至 0.8~1.0 則代表陷入懸空厚殼。
2. **`Floater %`（懸空不透明點佔比）**：越低代表前層浮殼被清得越乾淨。

---
我直接用web viewer看 ACD的結果都差不多 都是正常區塊有部分重建 大部分區塊都是floater 而問題區塊有大量floater 非常多
而B 他在正常部分重建不錯 沒什麼floater floater幾乎都在場景外（很大的） 但結構破碎 等於說其他結果用floater佔滿的地方 他都是破洞 這是不是代表什麼

--- 
那之後 我們啟動了 speed3_sfm 還在跑
