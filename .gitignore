.idea
.history
.vscode/
data
output*
camera_paths
renders
videos
edited
**/__pycache__
submodules/simple-knn
submodules/tiny-cuda-nn-fp32
submodules/diff-surfel-rasterization
submodules/diff-surfel-rasterization_debug
internal/dataparsers/colmap_cluster_dataparser.py
internal/utils/data_sampler.py
tools/merge_new_img.py
tools/render_clip.py
scripts/experiments
notebooks_dumped
clusters
segments
**/__pycache__
upload.sh
# ⚠ 原本是 `*video*`，那會連**原始碼**一起擋（blender/generate_video.py、doc/render_video.md）。
# 只擋影片檔本身。
*.mp4
*.mov
*.avi
videos/
*png*

# ── 2026-08-06：建立 git 安全網時補上 ──────────────────────────────
# 沒有這些規則的話 `git add -A` 會吞掉 455G 的 outputs 與 1.3G 的 DA2 權重。
outputs/
logs/
# 下載的論文 PDF，841M（47 檔，最大單檔 60.5MB），且非我方著作
# ⚠ 原本寫成 `參考論文/   # 註解` —— .gitignore 的 `#` **只有行首才是註解**，
#   所以那個 pattern 是整行字串、match 不到任何東西 => 它們一直被追蹤著。
參考論文/
*.ckpt
*.ply
*.pth
*.npy
*.pt
utils/Depth-Anything-V2/checkpoints/
__pycache__/
*.pyc
build/
*.egg-info/
submodules/.rasterizer_upstream_git/
*.egg-info/
# 佇列是**每台機器的執行期狀態**，不是原始碼。
# 2026-09-13：它被追蹤時，我從 Jupyter API 改 lab 的佇列 => 每次 git pull 都
# 「Your local changes would be overwritten」而中止（而我還把那個錯誤靜音過一次）。
scripts/queue.txt

# 機器標記檔（scripts/lab/_common.sh 用它判 lab vs 本機）—— 每台機器自己建，不進 git
.lab_machine

# ⛔ 含 lab Jupyter token 的個人筆記 —— 絕對不可進 git（2026-09-20 差點被 git add -A 收進去）
抓取.txt

# ── lab 端的本機狀態（2026-09-20 全同步時整理）────────────────────────────
# Jupyter 自動存檔；lab 上到處都是，不該進 repo
.ipynb_checkpoints/
# 佇列是逐機器的可變狀態（runner.sh:226 已容錯，缺檔只會 idle）
scripts/queue.bak_*
scripts/queue.stop
# lab 機器的識別標記，只能存在於 lab
.lab_machine
# 改光柵器前的就地備份
submodules/diff-surfel-rasterization-trim-pp/_bak_*/
# lab 的背景工作 log/pid
labrun/
# 切換到 repo 附帶 glm 時移開的 lab 舊 clone
submodules/diff-surfel-rasterization-trim-pp/third_party/glm.labclone_*/
