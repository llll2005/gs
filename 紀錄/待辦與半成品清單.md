# 待辦與半成品清單（持久追蹤，2026-07-24 起）

> **為何是檔案不是 task 工具**：2026-07-24 發現 task-tracking MCP 工具會斷線（昨天建的 #17-21
> 隨 MCP 斷線消失）。改用本檔——跨 session、跨工具斷線都不丟。**新待辦一律寫這裡。**
> 狀態：🔵跑中/排隊 · 🟡已實作待跑 · 📐設計未實作 · 🧪提議未跑 · ⛔阻塞(等前置) · ✅完成 · ⚫排除

## ⛔ 最高優先（2026-07-31）
**reg000 的幾何是浮空殼**（懸空不透明 78.7% vs A' 的 1.0%），其 +1.04 dB 是 `val⊂train` 的作弊。
**基於它的結論全部待重驗**。進行中：`opacity_reg` 掃描（0.007/0.002/0.0005）+ coarse-init 兩臂，
由 `scripts/runner.sh` 自動接力，**判準是 `tools/audit_geometry.py` 的「懸空且不透明」比例不是 PSNR**。
回來先看：`tail -30 logs/runner.log` 與 `grep 懸空 logs/runner.log`。

## A. GPU 隊列（單卡序列，會自動接力）
| 順位 | 實驗 | 狀態 | 判讀/依據 |
|---|---|---|---|
| **0** | **★★我方 coarse → 官方微調**（使用者 2026-08-06 提議）| **📐待 `official_depthinit_blk5` 開獎後拍板** | 填上 pipeline 比較表唯一空著的格子（官方 coarse 23.342 ✓ ／**我方 coarse ?** ／depth-init 跑中）。**⚠ 兩個必要修正**：①**模型必須用 `Gaussian2D` 不能用 `Gaussian2DSB`**——`gaussian_splatting.py:180-215` 的 ckpt 路徑會**連模型與 renderer 一起換掉**，SB coarse 會讓官方微調安靜地變成 SB 跑次，變數就不只 coarse 了 ②**24k + interval 350 是淨衰減**（破平衡 231.5），單塊剩 23%，全域套用會太稀（官方 coarse 是 499,682）⇒ interval 調到 231 附近或拉長 `densify_until_iter`，讓它落在 50 萬上下。**為什麼要等**：`official_depthinit_blk5` 會先回答「depth-init 直接微調打不打得贏 coarse-init」——若 depth-init 贏，coarse 這步可省，本項價值大降；若 coarse 贏，本項升為主線且知道要贏多少。成本 6~10h |
| **0'** | **v1clean_24k_b12** | **🔵已排隊**（接在 official_depthinit_blk5 之後）| P7 傷害量化 + **★「少而準」的正確測法**（v1 排程自然衰減到 77,616 顆且 densify 全程正常，不像 cap80k 把 densify 關死）。判準＝建築紋理比，對照 oreg 900k 的 0.372、汙染 v1 的 0.170、剪枝不微調的 0.266 |
| **0''** | 剪枝 + 微調 | 📐待 0' | 分離「壓縮」與「直接訓練」。⚠ 事後剪枝**不重新擬合**⇒ 測不到「小模型能不能表現場景」（使用者指出，見 `剪枝曲線與五個自我否證_2026-08-06.md` §二）|
| — | ⛔**cap_max 設在低於現有顆數** | **已否決 08-06** | `num_gs = max(0, min(cap,1.05N)-N)` ⇒ N 碰到 cap 就 **densify 永久關閉**，但 relocation/trim 照跑＝只剩破壞沒有建設。`cap80k_60k_b12` 就算沒 P7 也測不出東西 |
| **0** | **★★Stage 0：信心分級初始化**（b12, k=4, 24k）| **🔵跑中**（08-05 夜發車）| `scripts/run_graded_k4_24k_b12.sh`。錨點 203,916(15.5%) + 填充 1,112,307 = 1,316,223（depth-init 的 1.09×）。對照 `aggr24k_b12` @5,679 **20.644/.582/.764**，終點 22.070。**要量的不是 PSNR 是①錨點漂移②錨點存活率**（`tools/measure_anchor_drift.py`）。權威＝`信心分級初始化_2026-08-05.md` |
| 0a | Stage 1：`w_i` → 差別化位置 lr + 剪枝豁免 | 📐待 Stage 0 漂移讀數 | 沿用 `rtg_stable_density_controller.py` 的 `eta` 管路。漂移讀數決定它是否必要與參數強度 |
| 0b | Stage 2：Tier B 預算改用螢幕足跡分配 | 📐設計未實作 | 真正的 cost-aware init。⚠ 實測 depth-init 的 scale **全部相同**（後備分支），顆數與尺寸都是 `c_i≡1` |
| — | ❌**純 SfM-init（image-filtered）** | **已否決 08-05** | `d_reg` 爆到 **5.5e5**（v1 同期 0.015）。因 `colmap_block_dataparser.py:152` 的 `selected_image_ids` 只留 320,513 顆（AABB 內其實有 1,309,511）→ 覆蓋不足 → 中位深度=0 → `1/(0+1e-8)`=1e8。**不是 metric 的錯是取點的錯**；`citygsv2_metrics.py:83` 的 `# TODO: apply mask` 是上游的，但**產生 1e8 的除法是我方加的**（上游讀 `outputs[depth_output_key]` 不做除法）|
| ~~0~~ | ~~S1：SfM-init 單臂 A/B~~（已被 Stage 0 取代）| — | **病灶改判：floater 根因在 init 不在剪枝**（`初始化溯源與SfM-init_2026-08-05.md`）。3DGS 論文 init 消融：init 不良的 floater「**cannot be removed by optimization**」⇒ 我方所有下游剪枝手段失敗是必然。**零成本**：拿 `b12_cap2m_reg000_exact` 配方（60k/cap2M/reg0/interval150/**val 23.39**）**只改 `--model.initialize_from null`**（dataparser 已帶 `selected_image_ids` 逐塊過濾，b12 得 320,513 顆；cap_max 不用改）。**step 5,680 比較基準 21.26**，追平就跑完、落後就砍。⚠ 非純單變數（位置＋顆數同時變，3.8× 少），報告要照實寫 |
| 0b | S2：SfM-init + 裁 p1–p99 離群點 | 📐待 S1 | SfM 點雲 z 最低 **-202**（地面 0.20/屋頂 2.14）＝COLMAP 壞點，會白佔 cap。裁了就多一個變數，故排 S1 之後 |
| 0c | S3：**depth loss 單獨消融**（B 項）| 📐待 S1 | ⚠**不可跟 depth-init 一起砍**：SfM 是點雲不是損失，只能說「靠近某點」，砍掉 depth loss 就沒有任何**稠密**幾何監督（SfM 稀疏處佔 88 萬顆）。`lambda_normal` 是 2DGS 自身法向、與偽深度無關，**別動** |
| 0d | S4：SfM 位置軟約束（單向、只在有點處生效）| 📐設計未實作 | 排 S1 之後：換 init 免費，寫 loss 要調權重試好幾組 |
| — | ❌**深度一致度當剪枝判準** | **已否決 08-05** | `tools/measure_depth_agreement.py`：r=+0.253、最好/最壞五分位只差 1.3×。**繼多視角集中度後第二個失敗的判準**。真因＝偽深度誤差 174× 表面解析度，比我方位置誤差(29×)還大 6 倍 ⇒ **深度監督已榨乾** |
| — | ✅**churn 消融**（opacity_reg=0）| **完成 07-28** | **★reg000 = 23.158 / 0% churn / 4.34 it/s / 1.97G**，vs A' 22.117 / 63% / 0.40 it/s → **+1.04dB 且快 10.9× 且省 3G，三軸全贏**。死亡跑步機是淨傷害 |
| 1.5a | ✅**`o≤1/255` 整顆剔** | **已驗·已採用** | 位元級 maxdiff **恰好 0**、剔 49% 的點、**peak render VRAM −20.3%**。🔵 正在測 b12@2M 能否活（之前死在 ~1.5M，8k 步約 15 分鐘）|
| 1.5b | **精確支撐半徑**（再 −32%）| ⚠**位元級 FAIL·待查** | peak −52.7%/Σtiles 3.45× 但 max 1.5e-3 差異；**+1px 邊界救不回 ⇒ 邊界精度假說被否證，真因未知**（待查 `computeAABB` 的 `extent` 語意）。即使查不出，仍可走「訓練 A/B 對 reg000 23.16」判斷可否用於訓練側 |
| — | ~~原 1.5~~ | 已拆成 1.5a/1.5b | `forward.cu` + `auxiliary.h` `EXACT_SUPPORT=1/GROW=0` 已寫入 submodule。**基準修正後估 8.71× 少相交 → N_max ×2.05（b12 1.70M→3.48M）⇒ 可能直接解開 b12@2M 且不需 tiling**（先前 4.30× 是誤以為 TIGHTBBOX 生效，其實 `#define TIGHTBBOX 0`＝一律平 3σ）。**無損可證**（forward.cu:408 / backward.cu:322 都已跳過 alpha<1/255，且保守界 min(·,3)）。**⚠ 必須等 DAR 跑完再重編**（換 .so 會影響跑中的 job）。驗證腳本 `scripts/verify_exact_support.sh`（缺 `tools/render_fixed_ckpt.py` 待寫）。PASS 後：重生 `block_caps.csv` → 重跑 b12@2M |
| 2 | **★DAR smoke**（1700 步，`scripts/run_dar_smoke.sh` **已排隊等 GPU**）| 🔵排隊中 | 驗 hook 觸發 + 實測 stride 對得上 CPU 預測（med~0.015-0.03 / max<0.25）。**stride[max] 若卡在 0.25 就是 clip 又綁住 → 停** |
| — | ❌**DAR 統一臂**（60k）| **完成·崩潰** | **PSNR 10.40 vs reg000 23.16**。崩因＝尺度崩塌(100% scale<1e-6)非 opacity；機制零 bug。兩個廉價後續分辨假說：(a)`scale_reg=0` (b)`dar_lambda=0.005`。詳 `現行方案與公式.md` §5 |
| 3 | ~~DAR 統一臂~~（已跑，見上）| ❌ | **統一框架第一個實證**。三端錨點：A'(c≡1)=22.12 / **reg000(無正則)=23.16 ←要贏的是這個** / DAR(cost-aware)。價值命題＝同品質但成本輪廓受控→塞更高 N |
| 4 | 壓縮排程（densify 30k 早停 + 收割拉長） | 🔵待排 | 方向與 churn 一致（**減少** 12k 步的 63% 處決）；若 45k 打平 60k → 25 塊省 25×15k 步 |
| 5 | b8 閉環（手設 1M vs **公式 cap 2.6M(讀 CSV)+動態 K**）| 🔵待排 | ★cap 自動算+K 自動調的端到端閉環；乖塊無怪物/無過載 |
| 6 | SH3@1.5M | 🔵待排（可砍） | SH3 的 count 響應斜率；價值最低 |
| 7 | **`K_max` 8→12 試打 b12@2M** | 🧪新提議 | 2026-07-28 算出 N≳1.7M 時 K=8 已飽和（預測峰值 2.92G>預算）；模型說 K=12 讓 2M 回到 2.71G。⚠先廉價實測 N≈1.7M 峰值 vs 預測，差值＝模型缺項（疑 densify 操作 O(N)，K 碰不到）|
| — | ⚫凝結退火 / v/c 判準 | 已跑，**方向錯** | 都是「加處決」→ 在 63% 過載的 b12 densify 期必 OOM。機制程式已修好，待低 churn 環境或乖塊重測 |

| 8 | **重測 importance prune**（補上 LightGaussian Eq.4 截斷後）| 🧪新提議 | 帳面最佳 24.47 是在**缺截斷**版本下拿的；修後應多剪大浮游物、少剪細幾何 → 同組態重測看是否再進步。廉價（沿用既有 aggr17+prune@30k 配方）|

| 9 | **高 N 掃 K 拆解峰值模型**（固定 load，N=1.7–2M 掃 K 擬合 `peak=fixed+splittable/K`）| 🧪新提議·★高價值 | **b12@2M 低估的頭號候選**：3DGS §6 明說 backward 重走 per-tile 排序陣列、不存 per-pixel list ⇒ 依原文沒有明顯 ∝N 項，但我方標定出 `B≈1550B/pt`。若其中一部分**不可被 K 分攤**（如每條帶都要投影全部 N 點的 O(N) 緩衝），高 N 就會低估固定成本。擬合出的 `fixed` 減去 `model_state+A·N` 即為錯放量 |

| 10 | **★修 relocation 共位問題**（Diet §3.2 的解法：回收點沿累積位置梯度加小位移）| 📐設計未實作·★高價值 | **攻死亡跑步機的機制而非症狀**。機制鏈：MCMC Eq.9 共位 + Eq.8 高 opacity 關噪聲 + Diet「共位 clone 梯度退化無法分化」→ 一起死 → 再 relocate。若成立，relocation 可從淨負轉正（對需要探索的塊才是真解）。廉價：`relocate_gs` 後對 `dead_indices` 加位移；對照組用切平面隨機小位移 |

| 11 | 矩形 AABB 取代正方 AABB（`forward.cu:241` 的 `max(ex,ey)`）| 🧪低優先 | **已查完 CUDA + 實測定量**：trim 用正方框，短軸被填成長軸。但加畫面裁切後**只省 1.14×（N_max ×1.08）**——總相交被「已蓋滿整幀」的點主導，正方/矩形都夾成整個 grid。**先前寫「可能是 b12 的解」已收回**。仍是 ~5 行 CUDA 的免費增益，但不緊急。精確橢圓相交上限也只有 4/π=1.27× |

| 12 | ⚠**過夜排程**（`scripts/run_overnight_queue.sh`）| ①②完成·③失敗待修 | ①densify 盲目度診斷(2k,只量不改) ②4×4 depth-init(→`depth_init_4x4/`,**不覆蓋 5×5**) ③4×4 單塊 60k(blk5,cap1.5M,reg000)。`estimated_depths` 逐影像不用重生 |
| — | ⚫**err_guided_densify** | **已否決（2026-07-29 診斷）** | `err@opacity-sampled/mean=1.000` 確認 MCMC 對誤差**完全盲目**，**但 `ceiling(top5%)/mean` 僅 1.2×** → 誤差分佈均勻，完美取樣器也只賺 20%。**程式保留（旗標=0）但不啟用**。★這個 ceiling 欄位擋下了一條原本被我方列為『價值/成本比最高』的路 |

## B. 已實作、待跑（程式在，未排或未驗）
- 🟡 `min_opacity` 退火（強制提早凝結）— 實作了沒跑；rationale 待確認後排。
- 🟡 `screen_prune_emergency_px`（per-step 緊急剪）— 實作了沒驗；動態 K 若還漏怪物才需要。
- 🟡 best-ckpt 追蹤（best_val.txt）— 實作了，現 no-op（held-out 才生效）。
- 🟡 DBP（DGD+cap+importance-prune）— 實作了沒跑（隨 grad-densify 線擱置，archive）。

## C. 設計了、未實作（只有數學/提案）
- 📐 雙峰 opacity / Main+Ghost 分裂 — Gemini 推了閉式解，沒寫程式（已歸檔；殭屍發現後優先序低）。
- 📐 ZH 帶諧函數 / SB sh_degree=1（補頻譜洞，SB 對 SH3 −0.33）— 提案，先看顆數能否補回再決定。
- 📐 2D tile backward（1D→2D，改 CUDA）— 低優先，K 已夠用。
- 📐 Adam-offload for SH3（SB 不划算，SH3 F=58 才值得）— 概念，未實作。

## D. 提議、需先做設計決策才能跑
- 🧪 cost-aware densify 乾淨對照（含 SSIM/LPIPS）— 需定義對照臂（同底座，MCMC relocate vs value-per-cost）。
  現有數字跨底座+PSNR-only 不能當乾淨評分表。**要跑前先決定對照設計。**

## E. 大魚（北極星，⛔ 阻塞在單塊配方定案）
- ⛔ 25 塊全場景 + merge → our-global vs 自跑 CityGSV2 — 等 SB+dynamic-K 單塊配方鎖定（dynk 決定）。
- ⛔ 自跑 CityGSV2 25 塊 baseline — 北極星的對照組。
- ⛔ held-out 評測（split_mode=experiment）— 方法論定了未執行；等配方 + 是 eval 非 train。
- （前置解鎖後，配方裡含：逐塊 cap 校準 + dynamic-K + cull_dust 部署壓縮）

## 維護規則
- 實驗跑完 → 更新順位狀態 + 結果進 `實驗總表.csv`。
- 新構想 → 按 A-E 分類寫這裡，別只放對話裡（會消失）。
- 阻塞解除（如 dynk 定案）→ 把 E 的大魚往 A 隊列搬。
