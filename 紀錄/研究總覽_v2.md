# 研究總覽 v2 —— 單一來源（2026-09-13 重寫中）

> ⚠ 這份正在建置。已完成的章節見下方；未完成的暫時仍以 `研究總覽.md` 為準。
> 大綱與搬遷規則：`_新紀錄大綱_2026-09-13.md`。判準：`倖存清單_2026-09-13.md`。

---
id: S0-now
狀態: 有效
年代: 修正後(2026-09-13~)
摘要: 現在在跑什麼、最近三個結論、下一步。這是唯一會頻繁改的一節。
時間: 2026-09-13
---

## §0 現況一頁

### 最近三個結論

```
1 失敗區的根因是**影像↔姿態錯位**，修好後基本消失
  舊資料 25.5% -> 15k 步 4.9% -> 60k 步 **1.79%**（同一絕對判準）    => §5
2 背包（成本感知的約束端）**有空間**，而且空間是「預算鬆緊」的函數
  rho(v,c)≈0（三次量測）；預算 10% 時上界 +48.7~57.4%，75% 時 +0.8~2.3%   => §1.3
3 `max_split_size_mb:128` **要開**（重複試驗：不設 OOM 5/5、設了 0/5）
  代價 14~17%；而 cap 2.6M 正好卡在懸崖邊（2.4M 以下不需要它）        => §2.2
```

### 跑中 / 待跑

```
lab   cs_base / cs_costdir（2 萬步對比，b6）・speed3 b13 60k
      待跑 16：cs_dssim05/both/costtaming、depthprep、cb50/cb25、initcmp depth、
              elong ×4、官方 741 held-out ×2、[solo] allocfrag、[solo] citygs_repro
本機  sfmfill 的 1,500 步閘門（PLY 產出中）
```

### 第一個完整 60k（修正後資料）

```
lab/speed3 b6   60,000 步   PSNR 30.4520 / SSIM 0.888 / LPIPS 0.126 / 紋理比 0.795   N=2.34M
軌跡 10,960 24.13 -> 21,920 26.65 -> 32,880 29.15 -> 43,840 30.02 -> 54,800 30.42 -> 60,000 30.45
```
⚠ val⊂train、單塊 => **不可**與論文的 27.46/27.23（合併 25 塊 held-out）相比。見 §6。

---
id: S7-method
狀態: 有效
年代: **不受年代分界影響**（方法論不經過 GT 影像↔姿態的配對）
摘要: >
  這一節是代價最昂貴的一類內容 —— 每一條都來自一次實際的損失（燒掉的 GPU 小時、
  作廢的結論、白追的方向）。它們不依賴任何資料年代，所以三次年代分界都沒有動到它們。
量測: 各條目自帶出處
時間: 2026-05 ~ 2026-09-13
---

## §7 方法論

### §7.1 量測

**先量再解釋。** 提出機制解釋之前先問「它預測什麼可量的量」。
2026-09 有一輪四個假說全部從一兩張圖跳到機制、**全被推翻**；而讀碼猜的三個減計算候選
**全沒進前 40 名**，真兇是沒想到的 trim pass（80%）。
⇒ **診斷能拒絕整個家族，介入一次只能測一個點。**

**判準要對準假設中的作用機制，不是方便量的東西。**
`oregstop` 的前提「收割期 opacity L1 是純損失」是用**死亡率**判的，
漏掉它真正在做的事：同樣 2.34M 顆，關掉後總 opacity 質量 +47%、o>=0.9 多 1.82 倍。

**隨機事件一律用重複試驗報比例，不准用單次。**
2026-09-13 我為 `max_split` 三次改口，全因單點量測：同一設定（N=2.6M、ballast 0.8G、
不開旗標）在六次試驗裡活過一次，我剛好抽到那次就宣稱「牆是隨機的」。
5 次重複後定案：不設 **OOM 5/5**、設了 **0/5**。

**比較前 diff 兩份 resolved config。**
`speed3_sfminit_b12` 以為唯一變數是 init，實際差**四項**，其中 `dynamic_strips=true` 讓
77.7% 的訓練步跑在被改過的 loss 上 => 一整趟 9 小時 GPU 作廢，而檢查是**秒級**的。
⇒ 已自動化：`tools/diff_resolved_config.py`，`run_fit` 收尾自動跑。
⚠ 讀的要是 **resolved config**（Lightning 寫下的實際生效值）—— CLI 旗標打錯字會被
  jsonargparse 吃掉而不報錯，只有它看得出來。

**比分數之前先驗跑次有沒有跑完。**
`results.txt` **不寫步數** => 半途 OOM 的跑次會留下看起來正常的分數。

**驗「有沒有異常值」要取 max 與計數，不能取樣。**
P7 的 `1/(surf_depth+1e-8)` 在無粒子像素回傳 **1e8**，取樣看不到。

**「A 對應到 B」的驗證，判準不能來自 A 或 B。**
2026-09-13 我用三個不足的測試撤回了一個**正確**的發現：
(a) sparse 對 transforms.json 是循環（同一組姿態）
(b) 對極幾何對「一致的幀偏移」不敏感
(c) 「模型重建得好」與過擬合相容 —— **這一點是使用者指出的**
⇒ 最後是**位元組級 md5** 定案。三個不足的測試不會相加成一個足夠的測試。

**先量便宜的再投昂貴的。** 6GB 單卡序列，一個實驗鎖 4~11 小時。
2026-09-13 的例子：背包的「有沒有空間」用**一次前向**就答完（`tools/rho_value_cost.py`），
而原本的計畫是跑 60k；`cost_budget` 的標定同理，省下約 15 小時 lab 時間。

**上界要標成上界。** `rho_value_cost` 的 +57% 是離線、且知道每顆真實 v 的選擇；
訓練期只能用代理訊號 => 實際一定更少。不標清楚就會變成 overclaim。

### §7.2 基建

**共同形狀：這些失敗都不會報錯。**
已抓到 7 個「看起來正常但沒作用」的機制（死碼／守衛不含自己／ckpt 覆蓋 renderer 旗標／
步數不接續／0 秒 exit 1／test 分數被丟棄／`ColmapBlock.instantiate` 回傳基底 parser）。
2026-09-13 再加三個：
```
腳本被原地覆寫   bash **逐段讀取**腳本檔 => 跑到一半被 git pull／編輯器覆寫，
                 會從錯的位元組偏移繼續讀。實測後果是**靜默吃掉剩下的腳本且 rc=0**。
                 => scripts/_noedit.sh（讀進記憶體再執行，$0 設回原路徑）
下載被中斷       半個檔案留在最終檔名下，事後沒有東西比對大小 => 載入時才炸。
                 => 先寫 .part、比對大小、通過才原子改名
工具雙開         兩個行程寫同一個輸出目錄 => 產出的是**舊參數**的結果，
                 而檔案大小一模一樣。沒有錯誤、沒有崩潰，只是內容不是你以為的那個。
                 => internal/utils/outlock.py，且鎖要在**載入任何東西之前**取（快速失敗）
```

**新機制必須印「✅ 首次觸發 + 實現的參數範圍」或「⚠⚠ 拿不到訊號」。**
2026-09-07 連救三次。配套：「沒效果」的結論要先出示它**觸發過**。

**排程一律用 `scripts/runner.sh` + `queue.txt`，不准自開 watcher。**
`pgrep` 判 GPU 三種踩法全中過（cmdline 被 `conda run` 改寫／匹配到檢查自己的 shell／
殭屍照樣匹配）。2026-09-13 我用 `pkill -f 'lab.py pull'` 又中第二種 —— **把執行該指令的
shell 自己殺掉**（exit 144）。⇒ 要停行程用 **PID**，不要用會匹配到自己的 pattern。

**改正在執行的 `.sh` 一律用「寫暫存檔再 mv」**（新 inode），不要用 Python 就地覆寫。
實測 `sed -i` 換 inode、`open(w)` 不換。

**監看的判準要對準「活著」的直接訊號。**
「runner.log 沒成長」不等於死了 —— 它只在 START/DONE/FAIL 時寫，三個槽都在跑長跑次時
本來就不會成長。改看**跑次步數是否前進**。
同理：單次 API 逾時不算掛掉（我自己的大量下載就會把它塞住）=> 重試一次再判定。

### §7.3 這一節為什麼不受年代分界影響

判準只有一條：**這個量測有沒有經過「GT 影像↔姿態」的配對？**
方法論講的是「怎麼量」「怎麼排程」「什麼形狀的失敗不會報錯」，都不碰那個配對
=> 三次年代分界都沒有動到它們。這也是搬遷時先搬這一節的原因：**沒有判斷風險**。

---
id: S2-envelope
狀態: 有效
年代: 修正後(2026-09-13~)
摘要: >
  6GB 不是外加的限制，是命題的主場 —— 背包的空間只在**緊預算**下才大（§1.3）。
  這一節是這個信封的物理形狀：牆在哪、為什麼、哪些旋鈕真的換得到餘裕。
資料: logs/maxsplit_verdict_*.log, logs/oom_wall_repeats_*.log, logs/cap_vs_maxsplit_*.log
組態: N 固定、只改餘裕（tools/vram_pressure.py --arm b）
量測: 每個點 3~5 次重複試驗；計算量逐字相同，時間變化只能來自配置器
依賴: [S7-method]
時間: 2026-09-11 ~ 2026-09-13
---

## §2 6GB 信封

### §2.1 VRAM 是硬牆不是慢坡

N 固定、只改可用餘裕：每步時間 **0.999~1.001x 平到撞牆**，`num_alloc_retries` 一路 0。
⇒ **慢不是配置器在搬**。純計算 ≈ 155 ms/百萬顆 + 21 ms，而全訓練的固定項是 179 ms
⇒ 那 158 ms 的差額在 dataloader／全解析度 loss／Lightning，**不在 render**。

### §2.2 配置器：`max_split_size_mb:128` 要開（定案）

```
N=2.6M、ballast 0.8G、每臂 5 次
  不設                OOM **5/5**
  max_split_size_mb:128  OOM **0/5**   retries 13~15，314~320 ms
```
代價在新資料上是 **14~17%**（263.0 -> 300.0 ms 無壓力），不是舊資料時記的 6.4%。
對 8.8h 的跑次 = 75~90 分鐘，換掉「83% 機率整趟死」—— 划算。

**⚠ 但懸崖很陡，而 `cap_max 2.6M` 正好卡在邊上：**
```
N=2.0M / 2.2M / 2.4M   不開也 0 OOM（0/5、0/3、0/3）
N=2.6M                 不開 5/5 死
```
```
cap 2.6M + max_split    ~317 ms   非有旗標不可
cap 2.4M 不開            252 ms   快 20%
cap 2.0M 不開            212 ms   快 33%，而且剩餘餘裕更大（0.763G vs 0.429G）
```
⇒ 「2.6M 值不值得多花 20%」是**品質問題**，要用 2 萬步對比骨架答，不是再量 VRAM。

**「保留 − 配置」不等於碎片。** 壓力上升時那個落差會自己縮小（0.878 -> 0.272G），
因為配置器被逼著釋放快取 ⇒ 低壓時的落差**大多是可回收的快取**。
真正的失敗是「連回收完都湊不出一塊連續的大記憶體」，而 `num_alloc_retries` 正是在數這件事
（retries 13~15 卻只慢 4.5% ⇒ 那個「釋放再重試」的機制很便宜，它就是 +0.6GB 的來源）。

⚠ `expandable_segments` 在 torch 2.0.1 不支援。

### §2.3 上限旗標的邊界

`CITYGS_VRAM_CAP_GB` 用 `torch.cuda.set_per_process_memory_fraction`，只管 **PyTorch 配置器**。
實測 lab 上兩個行程各佔 **6,084 / 6,068 MiB**，而上限是 5,796 MiB（5.66 GiB）⇒ 超出約 **280 MiB**
（光柵器自己 `cudaMalloc` 的部分 + CUDA context 不受管）。
⇒ **那個上限是下界**，「塞不塞得進 6GB」的最終宣稱要在真的 6.1 GiB 卡上量。

### §2.4 時間花在哪

不是搬運（4.2%）：backward 37% ／ forward 24% ／ **週期性 trim 21%（densify 期）** ／ optimizer 10.5%。
⚠ cProfile 說 `Tensor.to` 佔 71% 是**同步點假象**，實際算術只有 0.14 秒。
  計時要每個標記點同步；週期性事件要按**真實週期**攤平，不是按取樣視窗。

---
id: S5-spatial
狀態: 有效（線已收）
年代: 修正後(2026-09-13~)
摘要: >
  「畫面上有一塊糊掉」這個現象從 2026-09-03 追到 09-13，期間試過殼層、init、
  SfM 特徵密度、優化失敗、機制交互…… 全部不是。根因是**影像↔姿態錯位**（§3.1），
  修好後從 25.5% 掉到 1.79%，而且長跑後更乾淨。
資料: outputs/gate15000/blocks/block_12, outputs/speed3/blocks/block_13, outputs/lab/speed3/blocks/block_6
組態: 絕對對比門檻 GT std >= 0.1 ／ corr < 0.6 ／ tile 48px ／ SfM 離群過濾 |z-0.26| <= 5.0
量測: tools/failure_map.py；69~84 張 val 影像／11,952~17,382 個通過門檻的 tile
依賴: [S3-data-pairing]
時間: 2026-09-03 ~ 2026-09-13
---

## §5 重建品質的空間分布

```
舊資料（同一絕對判準）        25.5%
新資料 15,000 步  b12 4.92%   b13 4.88%
新資料 60,000 步  b6  **1.79%**   corr 中位 0.983   紋理比 0.986（失敗 0.358 / 通過 0.987）
```
⇒ **長跑後更乾淨**（4.9% -> 1.79%）=> 15k 的乾淨不是短跑造成的假象。
⇒ 失敗率地圖：**內部整片 0，失敗只在邊緣格**（那裡觀測相機少，是正常現象）；
  舊資料上那是**左半邊一整塊**。

### ⚠ 判準本身曾被汙染過，引用糊掉率一定要附門檻

相對門檻（`GT std > quantile(sg, 0.40)`）會把平坦 tile 收進來，那裡的 corr 是噪音
=> 同一批資料會從 28% 被講成 46%。**一律用絕對門檻並寫出門檻值。**

### 被這條線否證掉的假說（都不必再試）

```
糊掉 = SfM 無特徵區        ✗ 糊掉 tile 的觀測**多** 12.5%、track **長** 10%
糊掉 = 那裡沒資訊          ✗ 60k 上糊掉 tile 的覆蓋與沒糊的相同
糊掉 = 位移／對位偏差       ✗ 最佳位移只提升 +0.031（清楚 tile 0.000）
糊掉 = 我方機制造成         ✗ 零機制的 vanilla 也一樣糊
糊掉 = 泛化落差            ✗ val ⊂ train，訓練時看過那些影像仍重建不出 => 是**擬合失敗**
```
⇒ 五個都指向「輸入有問題」，而當時沒人想到是**影像對到別的姿態**。

---
id: S6-eval
狀態: 有效
年代: 跨年代（協定本身不依賴資料）
摘要: >
  什麼數字可以跟什麼比。這一節的存在是因為本專案**反覆**用不可比的數字做決策，
  而每次都要事後撤回。
依賴: [S3-data-testset]
時間: 2026-08-01 ~ 2026-09-13
---

## §6 評測協定

```
val ⊂ train（reconstruction split）   我方絕大多數數字。**內部可比，不是泛化**
官方 741 幀 held-out                  唯一跨年代可比的；官方協定評的是**合併 25 塊**
單塊的官方 test                        是**下界**（單塊只佔畫面一小部分，PSNR 卡 ~15dB 與好壞無關）
```

### 三個「看起來可比其實不可比」的陷阱

```
跨年代    資料換過三次（SfM 座標系／GT 錯位／影像↔姿態）=> 舊分數一律不可引用
跨塊      b6/b7/b12/b13 的內容差很多，增益是塊相依的
跨相機數  **量尺會跟著變**：b12 從 284 台變 653 台 => val 是它的子集
          => 連「同一塊的新舊 val」都不可比，因為評分用的是**不同的影像**
```
⇒ 2026-09-13 實例：舊 `speed3_b12` best_val 26.9258@56,800（284 台）vs
  新 `gate15000` 24.6726@13,060（653 台）—— 這兩個數字放在一起**沒有意義**。

### held-out 曾經翻轉過排序

零機制的 vanilla（1500 步）held-out **17.869 / 0.4815** 贏 speed3 的 16.969 / 0.4616，
而 val⊂train 上它輸 5.2 dB。成因：val 的評分面積有 63% 在本塊 AABB 之外，
單塊模型用 floater 解釋鄰塊內容（訓練視角降 loss、新視角是垃圾）。
⚠ n=30／單塊／顆數差 3.8 倍／**LPIPS 反向** => 不可宣稱配方有害，但**足以證明 val 排序不可信**。

---
id: S1-thesis
狀態: 進行中
年代: 修正後(2026-09-13~)
摘要: >
  命題：既有的密度控制**用顆數計價**（cap_max、opacity L1、scale L1），對「這顆實際上
  多貴」是盲的；我方改用渲染成本計價，解一個資源受限問題。
  現況：**取樣端**在寬鬆預算下輸（但那正是天花板只有 2~9% 的區間）；**約束端**進行中；
  而「背包到底有沒有空間」2026-09-13 第一次被直接量出來 —— 有，且空間隨預算收緊而放大。
來源論文: [3DGS-MCMC, AbsGS, Taming3DGS, RAIN-GS, CityGaussianV2]
依賴: [S2-envelope, S7-method]
時間: 2026-07 ~ 2026-09-13
---

## §1 命題：成本感知的密度控制

```
max Q(θ_S)   s.t.   (1/K)·Σ_{i∈S} c_i <= B(hardware)
  c_i = 螢幕 tile 足跡（VRAM 有據）   λ = 影子價格   K = 條帶切分（供給側槓桿）
```

⚠ **「每個先前方法都是 `c_i ≡ 1` 的特例」有兩個反例**，引用時必須說清楚：
```
RAIN-GS      `s = HW/(9πN)` 是全域螢幕預算
Taming 3DGS  densify 分數裡有 `c^i_g`＝該顆在該視角覆蓋的像素數
             ⚠ 但符號是**正的**（大足跡 -> 模糊 -> 更該增生），我方是除；
               且權重 0.1 對 ∇g 的 50 ＝約 0.2% 的分數 => **他們也沒真的測過那個方向**
```
⇒ 倖存的區別是 **c 當偵測器 vs c 當價格**（綁在 `Σc_i <= B` 上），不是「誰用了 c」。

### §1.1 取樣端：三變體全輸 —— 但那是在**寬鬆預算**下量的

2026-09-07 定案（29 小時 GPU）：符號 ±／冪次+加法／代理+精確 c／強度校準，全輸，
基準（無成本項）仍最佳。共同形狀「紋理比↑ 而 PSNR/SSIM/LPIPS↓」且**不是塵埃**
（floater 方向不一致）⇒ **虛假細節**。

**2026-09-13 的重讀：這個否證是在 `cap_max 2.6M` 下做的，而那是寬鬆端。**
背包上界在 50~75% 預算只有 **+0.8~8.9%**（§1.3）⇒ 它們是在幾乎沒東西可賺的區間量的，
還要付「取樣分布變差」的代價。⇒ **「它們輸」與「這個方向沒用」是兩回事。**

還有兩個重量後鬆動的前提：
```
代理 c vs 精確 c   Spearman 0.856~0.927／top10% 重疊 88.9~91.1%（工具判準：>0.95 且 >90% 才算夠好）
                    且代理把動態範圍**高估約 1.7 倍**（15.25 vs 8.58）
                    => 先前用代理得到的成本結論**不可直接沿用**
強度校準            令 `1 + w·ceiling` = 25.8（對齊 absgrad）=> w 應是 **4.0（1/c）/ 2.8（c）**
                    而被否證的冪次式實測跨度 0.083~25.0 = **300 倍** => 超出校準帶約 12 倍
                    => 疑點在**形式與強度**，不在訊號本身
```

### §1.2 約束端：`cost_budget` 取代 `cap_max`（進行中）

```
生長條件從   N <= cap_max
      換成   max_view Σ_i (2r_i/16)^2 <= cost_budget
```
依據：`cap_max` 對「貴的前半」與「幾乎免費的後半」收一樣的價 ——
實測 `15k -> 30k 顆數 +98% 而成本只 +9.4%`，模型在 step 15k 就付掉最終渲染成本的 91%、
卻只用了最終顆數的 56%。

★ 這也回答「最佳 N 會隨場景複雜度改變」這個反對（使用者 2026-09-13）：
**N 不再是我設的定值，而是從場景長出來。**

標定（`tools/cost_budget_calibrate.py`，**不需要訓練**，一次前向）：
```
b6  speed3@14999    N≈2.60M   B0 = 17,987,534   (B/N 6.92)
b12 gate15000@15000 N=2.34M   B0 = 14,177,821   (B/N 6.06)
```
⚠ 不可沿用 `tools/cost_budget_probe.py` 的 23.1M —— 同定義但**不同實作路徑**
  （解析投影 vs 光柵器 radii）。
⚠ `cap_max` 要保留當安全閥：VRAM = 逐顆儲存（只看 N）＋ binning（只看成本），
  本旗標只約束後者。

### §1.3 ★ 背包的空間：第一次被直接量出來（2026-09-13）

`tools/rho_value_cost.py` —— v（trim pass 的 per-primitive contribution，`reduce="max"`）與
c（`num_covered_pixels`）**在同一次前向裡算出來**，沒有對齊問題。

```
                 b12@15k   b13@15k   b6@60k
rho(v,c) Pearson  -0.0060   +0.0299   -0.0500
         Spearman +0.1237   +0.0644   -0.0331
v/c 的 ceiling      6.89x     6.78x     6.27x
top10% 重疊        53.94%    67.13%    59.60%    （按 v 選 vs 按 v/c 選；隨機 10%）
```
⇒ **v 與 c 解耦 ⇒ 退化定理（`v_i ~ kappa·c_i` ⇒ 選擇無自由度）的前提是假的。**
⚠ 這個量測**不經過 GT 影像配對** => 修正前記的 `+0.127` 與現在 b12 的 `+0.1237` 幾乎相同
  ⇒ 它一直都有效，屬於倖存類。

**而空間的大小是「預算鬆緊」的函數**（離線貪婪 by v/c 比 by v 多拿到的總價值）：
```
預算佔總成本   10%      25%      50%      75%
b12          +57.30%  +26.61%  + 8.89%  + 2.26%
b13          +57.36%  +22.34%  + 6.12%  + 1.12%
b6@60k       +48.70%  +21.50%  + 5.42%  + 0.78%
```
⇒ **6GB 是緊預算，那是命題的主場。** 介入要排在緊預算側（cb50 / cb25）。

⚠ 三個保留（引用前必看）：
```
① 這是**上界** —— 離線、且知道每顆的真實 v；訓練期只能用代理訊號
② 這是**事後選子集**，不是訓練期分配（「改變集合」有效、「破壞集合」-2.34 dB）
③ 「總價值 +57%」**不等於 PSNR +57%** —— v 是剪枝依據的目標，不是品質
```

### §1.4 SSIM 已經在按足跡計價，而且**方向相反**

固定總誤差量、只改它攤開的方式（1% 畫面、振幅 0.25，`tools/loss_footprint_pricing.py`，
純 CPU、不需模型）：
```
 半徑(px)   2      4      8     16     32     64
 SSIM/L1  14.75   8.08   4.53   2.44   1.45   1.05      ΔL1 幾乎不變（0.00184~0.00218）
```
⇒ **SSIM 按足跡的反比計價**：同樣總量的誤差，攤成很多小斑塊罰得比集中成一大塊重 **14 倍**。
⇒ 兩個推論：
```
①「c 在任何地方都沒被計價，所以我們該計價它」**不成立** ——
   目標函數裡本來就有很強的足跡相依項，只是**符號相反**。
   我方的 1/c（偏好小足跡）與它**同向**（可能冗餘）；Taming 的 +c 與它**相反**。
   => 可驗預測：提高 lambda_dssim 之後，1/c 的增益應該縮小或消失（2x2 已排入 lab）
② `dssim05` 讓 floater 1.193% -> 0.958% **不是**這個機制（floater 是大足跡，
   而提高 SSIM 權重反而更罰小足跡）=> 那個下降另有原因
```
⚠ 這量的是**損失對固定擾動的反應**，不是優化器拿它去做什麼 => 機制可信但未證。
⚠ 這支工具**先前從沒被跑過** —— 它少了 `sys.path` 兩行，一跑就 `ModuleNotFoundError`。

---
id: S3-data
狀態: 有效
年代: 修正後(2026-09-13~)
摘要: >
  資料是地基，而本專案在這裡錯過三次，每次都作廢一整批結論。第三次（影像↔姿態錯位）
  是最大的一次：86.64% 的影像對到別的姿態，而**所有指標看起來都正常**。
  這一節記錄三次的診斷、修法，以及現在的驗證關卡。
量測: tools/verify_pairing_geometric.py（C 段）、tools/verify_input_byte_level.py（md5）
依賴: [S7-method]
時間: 2026-05-29 ／ 2026-08-12 ／ 2026-09-13
---

## §3 資料

### §3.1 影像 ↔ 姿態錯位（第三次分界，2026-09-13）

```
frame_index in block_N/transforms.json  **就是檔案編號**
  （證明：transforms_origin.json 的幀數 == input/ 的檔案數，且連續 0..N-1）
相機 k 的姿態 = block_1..10 串接後的第 k 幀（殘差 **0.000e+00**，5621/5621）
但 input/00000k.png 裝的是該塊的**第 k 個循序檔案**（md5 位元組級確認）
=> 兩者對 **86.64%** 的影像不同
   b12：284 台只有 73 台（25.7%）正確；位移中位 **4.393 場景單位**，而 partition AABB 只有 2.86x2.59
```
**修法**：`input/NNNN.png` = `block_n/input/{frame_index:04d}.png`；
對照表在 `data/matrix_city/aerial/global_frame_map_correct.json`。
四位數命名（＝ SfM 相機名）讓 `colmap_dataparser.py:350` 走**名稱優先**比對，
繞過先前掩蓋此 bug 的位置式 fallback。

**⚠ 驗證關卡只有一個有效**：
```
A 段（位置+朝向 vs transforms.json）  ✗ **抓不到** —— 兩者共用同一組姿態，是循環
C 段（SfM 2D 對應的亮度相關，門檻 0.95）  ✓ 唯一有效：壞資料 0.5099 -> 修好 0.9928
```
⇒ 重建資料後**C 段沒過就不要訓練**（`scripts/setup/{local,lab}_rebuild_input.sh` 第 5 步）。

新的分割相機數：**b6=548、b12=653、b13=667**（舊的壞資料 b12 只有 284）。

⚠ 還沒修：5 對重複姿態（0.09%，block_1 8/257 受影響）；b6/b12/b13 不受影響。

### §3.2 官方 test 集：741 幀就是完整的

```
pose/block_all/transforms_test.json（官方 HF 下載）  = **741** frames
block_all_test/sparse/0/images.bin                  = **741** 台相機
官方 10 個 block_N_test.tar 解出                     = **871** 張
```
多出來的 130 張**沒有姿態也沒有相機** => 根本無法評測，加進去只會讓數字與論文不可比。
⇒ **741 就是官方評測集**，不是任意子集。

`block_all_test` 是**我方自己串接**的（官方只提供分塊的 tar）。2026-09-13 在 lab 重建時，
串接順序**用 md5 逐檔比對驗證**而不是猜 —— 741 幀全部在官方 871 張裡唯一命中
（沒命中 0／命中多個 0），產生後再逐位元覆核一次（錯 0）。
⚠ 猜串接順序 = 重演 §3.1 的同一類錯。

`images_1.2` 是**改名層**，不是降採樣後的檔案：
```
train  images_1.2 -> input（目錄符號連結）
test   images_1.2/NNNN.png -> input/{int(NNNN):06d}.png（741 個符號連結，共 2.9MB）
1.2 倍是 **dataparser 載入時**算的（image_dir: input + down_sample_factor: 1.2）
```
⇒ **沒有「1.2 版」與「全解析度版」兩種跑次**，所有跑次（含舊年代）都是 1.2
⇒ 跑次名不需要也不該加 `1.2x` 後綴。

### §3.3 深度圖與 depth-init

`estimated_depths/` 是**逐影像、與分割無關**的（改 `block_dim` 不需重跑），也是最貴的一步
（5,621 張）。命名必須跟著 `input/` ⇒ 新年代是**四位數** `0000.png.npy`。
⚠ 對不上時程式**拒絕猜測**（不補零去對鄰幀）並停用深度監督 —— 那正是第二次分界的教訓
  寫進碼裡；行為是對的，但會在 log 洗出每張一行的警告。

⚠ `depth_init/` 的 PLY 是**從深度圖算的** => 舊年代那批是用錯幀的深度擺位置的，新資料上不可用。
⚠ `depth_init_blocks.py` 原本 `import open3d`，而 lab 容器沒有 `libX11.so.6`
  => 已改用 numpy 的等價 `voxel_down_sample`（對 open3d 0.18.0 驗證**逐位元等價**，
  7 種情形差值全為 0.000e+00）。**兩台一律走 numpy 路徑**，否則兩邊產出的 PLY 會不同。

---
id: S4-init
狀態: 進行中
年代: 修正後(2026-09-13~)
摘要: >
  起點決定很多。MCMC 的 relocate **並沒有**讓 init 變得無關緊要 ——
  隨機起點在 step 1 就被起始 trim 砍掉 74%，整趟只長到 cap 的 1.7%，比 SfM 少 30 倍。
資料: outputs/lab/init_sfm, outputs/lab/init_random（各 b6/b13，20,000 步）
組態: 除 init 外全部相同，且機制全關（absgrad 0／fast_noise false／densify_until 10,000）
量測: 逐 ckpt 讀 means 的長度
來源論文: [RTG-SLAM, 3DGS(init 消融)]
依賴: [S3-data]
時間: 2026-09-12 ~ 2026-09-13
---

## §4 初始化

```
                投入        step 499 存活       step 14,999      佔 cap 2.6M
init_random   100,000        25,599  (-74%)      43,253            1.7%
init_sfm      （SfM 點）     579,003            1,460,922          56.2%
```
⇒ 隨機臂在 **step 1** 就崩掉，之後整趟長不起來。使用者目視確認：隨機臂「大多沒有細節」，
  SfM 臂「完整重建、只有某些建築有點糊」。

### §4.1 為什麼隨機會失敗：**是範圍，不是點數**

`colmap_dataparser.py:532` 把隨機點撒在**半寬 3R 的立方體**（R = 相機到質心的最大距離），
而實際內容是貼地薄層：
```
random 立方體每軸跨度 64.90
SfM 主體（2.5~97.5 百分位）  x 14.58 (22%)   y 11.91 (18%)   z **2.00 (3.1%)**
=> 主體只佔立方體 **0.127%**
   撒 100,000 點只有約 **127** 個落在內容區；撒 1,000,000 也只有 1,272（SfM 有 3.83M）
   => 靠均勻亂撒追上要撒到**十億級**
```

### §4.2 起始 trim 對 init 類型的敏感度差 3,700 倍

```
SfM init    579,004 -> 579,003   被砍 **1 個（0.0002%）**
隨機 init   100,000 ->  25,599   被砍 **74%**
```
⇒ 舊記錄「存活數固定約 36.2 萬，與投入顆數無關」**只在同類型的 init 之間成立**
  （那是在兩種 depth-init PLY 之間量的，它們都大致貼著表面）。
  正確的說法是「存活數取決於**有多少點落在光柵器看得到支撐的地方**」。

### §4.3 SfM + 空處低密度補點（新，使用者 2026-09-13 提案）

`tools/make_sfm_fill_init.py`：Tier A = SfM 點（可加抖動複製）／Tier B = 只在**沒有 SfM 點的
體素**補點，密度是 SfM 區的某個比例。
```
--fill-ratio 0.10 --fill-voxel 0.15   b6 608,802（Tier B 29,798 ＝ 5.1%）
--fill-ratio 0.35 --fill-voxel 0.35   b6 635,627（Tier B 56,623 ＝ 9.8%）
```
⚠ 走 `--data.parser.points_from ply` 而**不是** `--model.initialize_from`：
  後者載入已存好的 Gaussian 模型（scale 由檔案決定），而 sfm 臂走 `setup_from_pcd`
  （scale = `log(sqrt(distCUDA2(點雲)))`，**從當下這組點算的**）
  => 用 Gaussian PLY 會多出「scale 初始化」這個變數，就不是單變數了。
✅ 自檢關卡：`--fill-ratio 0` 得 579,004，與 sfm 臂的 579,003 差 1 個點（就是起始 trim 砍的那個）。

---
id: S8-closed
狀態: 有效（清單本身）
年代: 混合 —— 每條各自標記
摘要: >
  已關閉的研究方向。★ 每條都附「**什麼條件下會重開**」——
  因為本專案有過一次代價很高的誤關：「error-guided densify」被以「天花板只有 1.2x」關掉，
  而那個 1.2x 是**我方誤差分數**的天花板，AbsGS 的 |g| 在同一統計上是 **16.68x**
  => 差點擋掉現行最佳配方的一半。**是訊號品質的問題，不是那個家族的問題。**
依賴: [S7-method]
時間: 2026-06 ~ 2026-09-13
---

## §8 已關閉的線

⚠ 「已關閉的線」是**研究方向的決策紀錄**，與 §5「重建品質的空間分布」（重建結果上的現象）
  是完全不同的東西，不要混。

```
DT-ADMM-GAT（原始提案）      關：對偶數學活下來了，但用在 VRAM 約束上（§1.2）
reactive 影子價格 λ           關：反應式控制在單卡序列上量不出來
半徑式怪物偵測                關：MCMC 已刪掉「怪物」這個命名對應的機制
gradient checkpointing        關：省 VRAM 但換到的時間在 6GB 上不划算
Scaffold / latent-MLP         關：與 2DGS 的表面元表示衝突
vpc_prune_frac                關：**-2.34 dB**。把低 v/c 的粒子搬走 = 破壞它們正在做的事
                                  ⇒ 「改變集合」有效、「破壞集合」無效
scale_reg = 0                 關：**-18sd**。它做的「塵埃」是它在頂住尺度上限的副產物
純時間壓縮 proxy              關：三個時間尺度不可能同時保住（`tools/proxy_verdict.py`）
notrim2                       關：b12 贏但 **b7 反轉** ⇒ 加的紋理位置不對
SB-lobe 軸                    關：SB-4 帶 +35% 顆粒六項全輸；自適應 lobe 的前提被否證
```

### 標為「待重開」的

```
成本感知取樣端      ⚠ 否證是在**寬鬆預算**下量的（§1.1）=> 緊預算側未測
coarse 當空間先驗   ⚠ 反證（官方 coarse 幾何 slope 只有 0.158）是**錯位資料**上量的 => 要重量
depth loss          ⚠ 關掉它的決定是用**舊的、汙染的 scales** 量的 => 值得用修正後的重測
```

---
id: S9-papers
狀態: 骨架（逐條回核中）
年代: 跨年代
摘要: >
  雙向索引：我方某個機制是從哪篇論文來的（引用時不 overclaim）／
  某篇論文的機制在我們這裡是死是活（讀新論文時能直接定位）。
量測: 權威 = 參考論文/（12 篇 PDF）＋ 記憶 reference_papers.md 的對照表
時間: 2026-09-13
---

## §9 論文交叉索引

| 論文 | 它提供的機制（原文形式） | 我方實作 | 狀態 | 章節 |
|---|---|---|---|---|
| 3DGS-MCMC | relocate 死粒子 + 加噪；`N_target = 1.05·N` | `mcmc_density_controller.py` | 採用中 | §1 |
| AbsGS | 絕對位置梯度 `\|g\|` 當 densify 訊號 | `absgrad_densify`（`probs = o·(1+w·ĝ)`） | **採用中（現行最佳的一部分）** | §1.1 |
| Taming 3DGS | densify 分數含 `c^i_g`＝覆蓋像素數，**符號為正**、權重 0.1 | `cost_add_densify` w<0 | 待測（cs_costtaming） | §1.1 |
| RAIN-GS | 全域螢幕預算 `s = HW/(9πN)` | 未實作 | 本線的反例 | §1 |
| RTG-SLAM | opacity 二元且 **lr_α = 0**；錯了不解鎖，改「在前面加一顆 α=0.1 的」 | `rtg_stable_density_controller.py`（`freeze_opacity`／`mc_*`） | 未測 | §4 |
| 2DGS | 表面元（2D Gaussian）表示 | `gaussian_2d.py` | 採用中 | §2 |
| CityGaussianV2 | DGD／**Elongation Filter**／Parallel Tuning | 前者無、後者換成 MCMC 後**沒有** elongation | 部分；elongation 已排入重測 | §1 |
| RaDe-GS | 深度正則設計 | `depth_loss_weight` | 目前關閉（權重 0），標為待重開 | §3.3, §8 |
| LightGaussian | 重要度剪枝 | `tools/` 的剪枝線 | 已核對並修過一個真缺漏 | §8 |

⚠ **公式欄寫原文的形式，我方的改動寫在「我方實作」欄** —— 兩者混在一起正是 overclaim 的來源
  （記憶 `paper_audit` 記過我方五個 overclaim，都是這樣抓出來的）。
⚠ 這張表現在是**骨架**：每一列都要回 `參考論文/` 與 §8（12 篇逐篇核對實作 vs 原文）核對過
  才能標成「已核」。**不可憑印象填。**
