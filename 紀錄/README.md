# 紀錄／單一入口（2026-08-21）

**讀的順序：`_ctx.md`（75 行，開場必讀）→ 本檔找路 → `研究總覽.md` 查正文。**
有衝突一律以 `研究總覽.md` 為準；`_ctx.md` 只是省 token 的提要。

---

## 一分鐘現況

```
現行最佳   sched30_b12   26.377 / 0.7711 / 0.3542   2.34M 顆   9.6 小時
           = cap 2.6M + densify_until 30k + opacity_reg 0.002 + lambda_normal 0 + depth_loss 0
           腳本 scripts/task_sched30.sh
論文錨點   CityGaussianV2 27.23（8×A100）—— ⚠ 我方是 val ⊂ train，不可直接比

★ 真正的瓶頸不是平均，是尾巴：
   最差視角跨五種配方只差 0.08 dB、45,000 步只改善 4.5%
   修好最差 25% = +1.55 dB（現行最強機制的 4.7 倍，超過到 27.23 的缺口）
```

## 活的文件（會更新）

| 檔案 | 用途 |
|---|---|
| **`_ctx.md`** | 開場提要：現況數字、噪音底、判準、程式陷阱、常用指令、鐵律 |
| **`研究總覽.md`** | **正文權威**。§1 命題／§2 現況／§3 理論／§5 判準／§7 已否證／§9 陷阱／§10 隊列 |
| `實驗總表.csv` | 全部實驗（377 列，含 era 與作廢標記）。`tools/mark_csv_eras.py` 重生 |
| `多指標總表_2026-08-21.txt` | 修正後跑完的跑次 × 五指標快照（`tools/audit_all.py` 產出） |
| `完整指令手冊.md` | 全流程指令 |
| `待辦與半成品清單.md` | 操作用的活清單 |

## 判準速查（動任何結論前）

```
噪音底  PSNR 0.0325  SSIM 0.00091  LPIPS 0.00200  紋理比 0.00314  低頻 0.00028
一律多指標          python tools/audit_all.py
尾巴（最重要）      python tools/tail_analysis.py
結構誤差            python tools/lowfreq_error.py / lowfreq_split.py
比較前過濾死跑次    python tools/run_status.py --runs ...
```
- ❌ **floater%** 與結構誤差**反向**（r=−0.739）
- ❌ **紋理比**與結構誤差**正相關**（r=+0.659）、可灌水 ⇒ 不可當優化目標
- ⚠ **每個配方都要看圖** —— 使用者目視發現的疊影，全套指標都沒抓到

## 世代分界（引用任何數字前）

```
舊資料集   < 2026-05-29              SfM 重生，座標系不同
GT 錯位    < 2026-08-12 10:08:37     每張影像對到鄰幀；對內容重的塊傷害 2.13 倍
修正後     >= 上述時刻開跑的跑次      ← 只有這些可比
```
⚠ 判世代用**最早檔案時間**（開跑時刻），不是 mtime。

## 歸檔（`archive/`，46 檔，歷史不是現況）

| 檔案 | 內容 |
|---|---|
| **`研究總覽_S12日誌_2026-08-12to21.md`** | GT 修正後十天的完整日誌（40 節）。**含大量後來被推翻的中間結論，數字不可直接引用** |
| `S11_成本模型_bug下量測_作廢.md` | bug 期的成本模型量測 |
| 其餘 44 檔 | 更早的方向文件、廢案、開發日誌 |

## 鐵律

只用當前資料／outputs 全是這台 6GB 跑的、不准質疑硬體／繁體中文＋ASCII 數學／
指令用腳本或單行／大實驗前先 CPU 驗前提／拿檔案當基準前先查來源／
排程只用 `scripts/runner.sh`＋`queue.txt`，不准自開 watcher。
