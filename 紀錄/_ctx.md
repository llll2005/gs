# _ctx —— 給 Claude 的精簡提要（2026-08-21）

正文是 `研究總覽.md`。這份只放「開場就該知道、且查一次要花很多 token」的東西。
**有衝突以 `研究總覽.md` 為準。** 更新本檔時只增刪要點，不要寫敘事。

## 一句話

6GB 筆電上的 city-scale 2DGS。論文命題＝**成本感知密度控制**：既有方法用顆數計價，
我方用**渲染成本 `c_i`（螢幕 tile 足跡）**計價並解 `max Q s.t. (1/K)Σc_i ≤ B`。

## ★★★★★ 目前最強的活線：**SfM-init**（2026-09-10，跑次進行中）

`speed3_sfminit_b12` = `speed3` 配方，**唯一變數 `--model.initialize_from null`**
（SfM 稀疏點 320,513 顆起步，取代 depth-init PLY 的 1,211,537）。cap/正則全同。
```
step      depth-init(speed3)      SfM-init            ΔPSNR
 5,680   21.95/0.628/0.148    22.67/0.662/0.252    **+0.72**
11,360   22.58/0.662/0.225    23.36/0.686/0.308    **+0.78**
17,040   23.03/0.683/0.285    23.64/0.703/0.355    **+0.61**   <- 7.5 sd
22,720   23.83/0.708/0.366    24.56/0.725/0.420    **+0.73**
28,400   24.29/0.723/0.401    24.66/0.735/0.444    +0.37
（PSNR/SSIM/紋理比；噪音底 PSNR sd = 0.081）
```
**三個指標每個檢查點同向。** 印證記憶 `init_is_root_cause`（depth-init 起點離表面 20.2×）。
⚠ 未完賽、只有 b12 ⇒ **依鐵律要 b7 也驗**；⚠ Δ 在 28,400 縮到 +0.37，**必須看終點**。

**1500 步清殼探針四臂**（同一結論的獨立佐證）：
```
Arm              N       PSNR    SSIM    LPIPS    紋理比
A 基準(depth)  0.28M    20.11   0.586   0.757    0.089
B **SfM**      0.33M  **20.78** **0.611** **0.622**  0.178   <- 三個光度指標全勝
C o=0.05       1.20M    19.55   0.593   0.778    0.214   <- 紋理比最高但其餘最差=過度增生簽名
D absgrad@100  0.47M    20.38   0.589   0.757    0.086   <- 幾乎等於 A
```
**★ 使用者 viewer 觀察 + 可驗假說**：A/C/D 被 floater 佔滿的地方，B 是**破洞**
⇒ 那些區域可能就是 **SfM 找不到特徵**的地方（depth-init 到處都放點所以「佔住」，SfM 沒點所以空）。
⇒ **若它們又剛好是那 40~46% 持續糊掉的 tile，整條糊掉調查就換了性質：
   不是優化失敗，是「那裡本來就沒資訊」** —— 這能解釋為何每個機制解釋都倒。
⬜ **未做的便宜驗法**：SfM 點密度圖 × 糊掉遮罩的重疊率／相關（純 CPU）。

## 現在的數字

```
★★ 2026-09-06 **現行最佳 = `speed3` 配方**：sched30 + absgrad 2.0 + **fast_noise + noise_gate_eps 1e-3**
          b12 = **26.6957**（vs agd2 26.5602，+4.9sd）   b7 = **25.1448**（vs agd2 25.1361，+0.3sd 平）
          **並且快 6.8%(b12) / 9.5%(b7)**，後 1/3 達 +10~11%（噪音成本 ∝ N）
          腳本 scripts/task_speed3.sh ／ task_speed3_b7.sh
          ⚠ 定位是 **Pareto 改善（更快 + 不更差）**，不是分數突破：
            b7 四項全平 ⇒ **不可宣稱 +0.13 dB 是普遍增益**（b12 專屬，同 notrim2 形狀）
          ⚠ 唯一低於門檻的指標是 b12 紋理比 -3.1sd，但**不複製到 b7**，
            且 b12 的糊掉率(46.08->45.93)與 floater(1.660->1.540)都更好 => 判為塵埃減少
前一代    sched30 + **absgrad_densify 2.0**（=`agd2`）  b12 26.5602 / b7 25.1361
更早      sched30 + **absgrad_densify 1.0**   腳本 scripts/task_absgrad_densify.sh
★ 2026-08-30 **已採用**：`absgrad_densify` **2.0**（b12 26.5602 / b7 25.1361）。SSIM/LPIPS 兩塊都顯著贏、無指標轉負；⚠ 但 PSNR 大增益**只在 b12**（+6.4sd vs b7 +1.5sd）⇒ 增益塊相依，不可宣稱普遍 +0.18 dB（§11.53）。
          b12 = **26.43**（vs sched30 的 26.29±0.08，+6.3sd）
          b7  = **25.28**（vs sched30 的 24.99，+10.0sd）  <- **兩塊都贏，四指標同向**
          ⚠ 需要光柵器以 `ABSGRAD 1` 編譯（cuda_rasterizer/auxiliary.h）
          ⚠ 唯一代價：b7 最差視角 17.83 -> 17.50。見 §11.47
前一代    sched30  cap2.6M / densify_until 30k / opacity_reg 0.002 / lambda_normal 0 / depth_loss 0
          b12 = 26.29 ± 0.08（n=3 同組態：26.377/26.271/26.219，過去引用的 26.377 是最大值）
⛔ notrim2（關 trim）b12 拿到 26.535 但 **b7 反轉**：PSNR 與建築低頻兩個結構判準都翻號
   （見 §11.24），只有 SSIM/LPIPS/紋理比同向 —— 那三個都在量「紋理有多少」。
   ⇒ 它是「加紋理」不是結構改善，**不可當配方**。
⛔ coarseft_b12 26.518 的歸因也連帶降級（其對照 notrim2 只在 b12 成立）。
論文錨點  CityGaussianV2 全域 27.23（8×A100，只有 PSNR）
```
⚠ **認定新最佳必須等兩塊都完賽**（2026-08-26 教訓：我在 b12 單塊就改了這張表）。
⚠ 我方全是 **val ⊂ train**，27.23 是留出測試集 ⇒ **方向對我方有利，不可直接比。**
現成腳本：`scripts/task_sched30.sh`（b12）／`task_sched30_b7.sh`（b7）。

## ⛔⛔⛔ 論文頭號機制**已被否證**（2026-09-07 定案，談命題前必讀）

**「用渲染成本 c_i 引導增生取樣」三個變體全輸，共 29 小時 GPU：**
```
              PSNR      SSIM     LPIPS    紋理比
agd2       26.5602   0.7755   0.3467   0.4902   <- 基準（**無成本項**，仍是最佳）
fpd        26.4260   0.7753   0.3502   0.5109   冪次 w=-0.5（Taming 向）  -4.8sd
cadd       26.3719   0.7725   0.3542   0.5069   **加法+精確c+夾住**       -6.8sd
cheap      26.2398   0.7696   0.3585   0.4968   冪次 w=+0.5（我方向）    -11.6sd
```
**符號（±）、形式（冪次/加法）、訊號（代理/精確）、強度（未校準/校準）全都測過，全輸。**

**★ 三者的共同形狀：紋理比↑ 而 PSNR/SSIM/LPIPS↓，且**不是**塵埃**
（floater 方向不一致：cheap 1.270%↓／cadd 1.596%↓／fpd 1.713%↑）
⇒ 較可能是**虛假細節**：導向小足跡 => 大量小粒子 => 高頻變多，但**不在誤差所在之處**。
⇒ 與 absgrad 的成功互補：**重要的是往「誤差在的地方」增生（|g| 有效），
   不是往「便宜或貴的地方」增生（c 兩向皆無效）。**

**命題的最終狀態**
```
✅ 成立  c_i 異質（ceiling 9.19x）／cap_max 與 opacity L1 按顆數計價
         ／`scale_reg` 是 r=0.977 的成本代理 ⇒「先前方法都是 c≡1」對它不成立
⛔ 否證  「c_i 引導**取樣**會提升品質」
❓ 未測  c_i 放進**約束**（`cost_budget` 取代 cap_max）= `max Q s.t. (1/K)Σc_i<=B` 的字面形式
⚠ 先驗  取樣端連輸三次 ⇒ 對約束端也該降期望，但那是**不同機制**，不可直接外推
```

## ⛔ 「紋理比 = 塵埃」的推廣已撤回（2026-09-07）
n=3 時 floater/紋理比/PSNR 完美單調，n=5 就破了（cheap 與 cadd 的 floater **下降**而紋理比**上升**）。
⇒ **保留操作規則「紋理比上升時必須同時報 floater」，但不可再說「紋理比 = 塵埃」。**

## 成本代理的品質（2026-09-07）
```
代理 `_max_radii2D²` vs 光柵器精確 `num_covered_pixels`（96 台相機）
  Spearman **0.927**（max 視角）／ top10% 重疊 **86.3%** ／ ceiling 15.26x vs **9.19x**
```
⇒ 排序大致對，但 **top10% 有 14% 不同、且離散度誇大 66%**。
⇒ **建立在代理上的百分比不可當精確值引用**（§11.102 的 82.27%、§11.28 的 rho(v,c) 等）。
⇒ 精確 c 是**免費**的：trim 每 500 步已在算，現行配方下算完就丟。

## 糊掉那 40~46%：**機制型解釋全部被排除**，病灶未定（細節見 §11.S13）

現象為真（五項管線檢查全過、GT 配對 corr 0.9997、**val ⊂ train ⇒ 擬合失敗非泛化落差**）。
**⛔ 全部倒掉的解釋**：表徵容量／梯度盲區⇒動不了（**Adam 對幅度尺度不變**，SNR 比 1.095x）／
粒子卡住（失敗區淨位移**大 1.32x**）／縮 footprint（oracle 半徑比 1.019）／步長／多視角衝突／
頻率平衡 loss（**打錯頻段**：每單位內容的誤差隨頻率單調遞減 7.22x→0.78x）。
**⛔ O0 oracle 的正面結論已降級/收回**（自由度比 14.7x；留出視角 −0.675 dB）。
**✅ 仍有效的量測**：半徑懸崖 >12.25px（corr 0.980→0.732）／absgrad 有 **40%** 是足跡偵測器／
失敗區在**每個驅動力指標上都佔優**（殘差 1.85x、|m| 1.53x、步長 1.09x、位移 1.32x）**然後還是糊的**。
⇒ **新方向見本檔最上面的 SfM-init 假說**（可能是「那裡沒資訊」而非「優化失敗」）。

## 省時旗標（2026-09-06 驗過，**預設關閉**，需要速度時才開）

```
_accumulate_error_score 閘門   ✅ 已預設關（`densify_blind_report=False`）—— 訓練行為**完全不變**
fast_noise                     🟨 代數等價（CPU 實測相對差 7.8e-8、法線分量 ~1e-9）
noise_gate_eps 1e-3            🟨 跳過 69% 粒子的噪音；b7 四項全平、b12 無複製的傷害
實際加速 **b12 +6.83%(n=211) ／ b7 +9.48%(n=312)**，且**省得越來越多**（後 1/3：+10.3%／+11.0%）
⚠ 我一度只取前 75 個 epoch 得到 +2.88% 並宣稱「CUDA-event 高估 2.6 倍」—— **錯的**。
  噪音成本 ∝ N，早期顆數少 ⇒ 只取前段會系統性低估。CUDA-event 預估 7.47% 實際準確度 91%。
```
✅ **2026-09-06 改判為採用**：兩塊都加速（+6.8%／+9.5%）、b7 四項全平、b12 無複製的傷害
   ⇒ 這是 Pareto 改善（品質同等或更好 + 明顯更快），不是只在特定跑次才划算的東西。

## 判準修正：紋理比上升時**必須同時報 floater**（2026-09-06）

現行配方家族上三個跑次完美單調：**floater ↑ ⟺ 紋理比 ↑ ⟺ PSNR ↓**
```
speed3 1.540%/0.4744/26.6957  ｜  agd2 1.660%/0.4902/26.5602  ｜  fpd 1.713%/0.5109/26.4260
```
⇒ **紋理比在此工作點主要量到塵埃**（floater 是高梯度物體）。兩者同向就不是紋理。
⚠ 不推翻 08-02 的「紋理比有鑑別力」—— 那是比較**不同 kernel** 時定的，工作點不同。

## 噪音底（2026-08-27 用 n=3 同組態重跑重新標定）

```
指標      樣本sd    不可宣稱門檻(3sd)    舊記載     舊值低估
PSNR      0.081        0.24            0.0325      4.9x
SSIM      0.0005       0.0015          0.00091     1.0x   <- 舊值正確，**最可靠的尺**
LPIPS     0.0025       0.0075          0.0020      2.4x
紋理比     0.015        0.045           0.00314     8.6x
```
來源＝三個功能完全相同的跑次（`sched30_b12` / `egd_b12`（旗標是死碼）/ `sched30rep_b12`），
PSNR 全距 0.1583。訓練本身混沌（光柵器 atomicAdd 累加順序不決定性）。
⚠ **`notrim2_b12` 的「新高」+0.158 與這個全距一模一樣** ⇒ 本 session 大量宣稱作廢，見 §11.38。
⚠ 低頻誤差尚未用 n=3 重標，暫用舊值 0.00028（可能同樣低估）。

## ★ 真正的瓶頸：尾巴（一切決策先看這個）

```
最差視角跨五種配方全距 0.08 dB（平均 0.33 / 中位 0.76 / 最好 0.88）
最差視角從 step 15,000 起 45,000 步只改善 4.5%（同期平均改善 39%）
修好最差 25% = +1.55 dB  <- 現行最強機制(sched30 +0.327)的 4.7 倍，且超過到 27.23 的缺口
```
⇒ **2026-08-23 定案：換起點也沒用。** 四種 init／trim 組態（汙染 depth-init／修正
depth-init／SfM-init／關起始 trim）最差視角全距僅 **0.15 dB**；跨六種配方的**逐視角
難度排名 Spearman ρ = 0.990**（1729 與 2999 永遠是最差兩名）
⇒ **尾巴是內容決定的，「調配方」與「換起點」兩整類介入都碰不到。**
⇒ 剩下的路：①搞清楚那些視角難在哪（內容層級，還沒做過）②對那些區域投不對稱資源
（對得上成本感知命題）③把「平均值對雙峰分布是爛摘要」本身當貢獻。
⇒ **所有實驗除平均外一律報最差 10%**（`tools/tail_analysis.py`）。

## 判準（2026-08-21 改判，六個最常誤用的地方）

1. **一律多指標**（`tools/audit_all.py`）：曾出現五個指標五個不同第一名。
2. **`geometry_health.py` 的 floater% 與結構誤差反向**（r=−0.739）⇒ 不可當結構判準。
3. **紋理比與結構誤差正相關**（r=+0.659）、可用拉高局部對比灌水 ⇒ 不可當優化目標。
4. **結構機制用 `tools/lowfreq_error.py` 判，不要用 PSNR**（小效應時解析度差約 3 倍）。
   跨配方大差異時 PSNR 才是有效代理（r=−0.885）。
5. **相關不是因果，不可預測單一介入**（我因此翻車兩次）。
6. **整體 PSNR 在 b12 被水面稀釋**（一半平坦水面，均勻色塊也 32-40 dB）。
   `tools/lowfreq_split.py` / `region_split_from_test.py` 可分水面／建築（純 CPU，訓練中可用）。

**★ 判準升級（2026-08-27，§11.40）：一律用 `tools/cmp_runs.py`（最後 4 個 val 點的平均），
不要用終點值。** 終點 PSNR sd 0.0807 -> 最後 4 點平均 **0.0277**（緊 2.9 倍），SSIM 緊 5 倍。
該估計量已三次自我驗證（同組態對照都落在 ±2sd，真效應 −18~+148sd）。
用終點值曾讓我把 `notrim2`（+10.8sd 真效應）誤判成噪音，**同一題翻案三次**。
⚠ 建築低頻／最差10%／疊影% 來自 test 圖（單一快照）**無法平均**，門檻要保守。

**⚠ 每個配方都要看圖。**（2026-08-26：疊影偵測器的第一版假設錯了，是**看圖**救回來的。）
7. **疊影＝半透明鬼影**：`tools/veil_detect.py`（逐 tile 對 GT 迴歸；corr 高+增益<1+偏移>0
   ＝結構還在但被洗淡；corr 低＝糊掉）。全域紋理比抓不到它，因為疊影與模糊方向相反會互相抵消。
   ⛔ **實測與配方無關**（b7 三配方 21.92/22.04/22.04）⇒ 密度控制超參數整類不對症，見 §11.25。

## ⛔⛔ depth-init PLY 汙染（2026-08-22 發現，未解決前不要下 init 相關結論）

`utils/depth_init_blocks.py:126` 有和 dataparser **一模一樣的 zfill off-by-one**，
而 2026-08-12 的修正只修了 dataparser。**`depth_init/*.ply`（2026-05-29）的每一顆點
都是用鄰幀的深度圖擺位置的**，實測位移中位 **6.18× 表面間距**。
⚠ **但殼的厚度幾乎沒變**（20.0× → 19.5×）⇒ **殼是單目偽深度的固有性質，不是這個 bug**；
bug 做的是「在同樣厚的殼裡把點打亂」。⛔ 我原本說的「35.6% 汙染 = 4.1 倍」量法錯誤已撤回
（比較相鄰深度圖的同一像素，但相機移動了，那是不同內容）。
✅ 程式已修，`depth_init_fix/` 重生中；`scripts/task_fixdepth.sh` 量它值多少 dB。
✅ 臂與臂的比較仍成立（同一混淆在每一臂，且監督是對的）；❌ 絕對值要重測。

## ⛔⛔ depth_init 已於 2026-09-12 就地替換為正確版（引用分數前必看）

```
舊 depth_init/*.ply（2026-05-29，每顆點用鄰幀深度擺位）**已刪除**
現 depth_init/*.ply = 原 depth_init_fix/（2026-08-22，位置對應正確），md5 64a9873b…
```
1. **所有既有 depth-init 跑次都是汙染起點**（speed3_b12 26.6957 等全部）。
   但 `fixdepth_b12` 實測差異 **PSNR −0.2sd 平／SSIM −10.7sd LOSE／殼只薄 2.6%**
   ⇒ **不作廢**，臂間比較仍成立，絕對值偏差在噪音級。
2. ⚠ **`task_speed3.sh` 不再能複現 `speed3_b12`** —— 路徑沒變但底下的 PLY 換了。
   要對照舊基準請用 `tools/cmp_runs.py` 比既有跑次，**不要重跑基準**。
3. 衍生變體（op05／sub05／uniform25m／graded_k4）已隨之刪除；那四條線都已退役，
   需要時可用 `reopacity_ply.py` / `make_uniform_init.py` / `make_graded_init.py` 從正確版重生。
詳見 `紀錄/研究總覽.md §16.7`、刪除清單 `紀錄/已刪除_汙染depth_init_2026-09-12.txt`。

## 十個踩過的程式陷阱

1. **⛔⛔ 用 ckpt 當 `initialize_from` 時，所有 `--model.renderer.init_args.*` 都會被丟棄**
   （`gaussian_splatting.py:183` 會用 ckpt 的 renderer 整個取代）。
   ⇒ `coarse_fix` 的 config 有 `diable_trimming: true`，所以 `coarseft`/`half30k`
   **完全沒有 trim**（顆數下降事件 0 次、最終 1.000xcap；有 trim 的是 59 次、0.900xcap），
   而我以為的「coarse-init 新高 +0.141」其實混了這個變數。**PLY 路徑不會有這個問題。**
2. **⛔ 不要同時開 `screen_size_prune_px` 與 `screen_prune_emergency_px`**：兩者讀同一個
   `_max_radii2D`，但 emergency 會把它從「光柵器實際半徑」覆寫成 `projected_radius()` 的
   **解析上界**（含相機後方的點）⇒ 300px 門檻變成每次 densify 刪 25% 族群。
   實測 `scrprune_b7` **PSNR 13.498**（對照 24.993）。單開 `screen_size_prune_px` 是安全的。
   ⛔ **2026-09-05 更正**：舊註「光柵器半徑從沒超過 300px ⇒ 等於無作用」**是錯的**。
   `agd2_b12` 單次跑次實測：觸發 **193 次**（每個 densify 事件）、總回收 **189,433 顆
   = cap 的 7.29%**、觀測到的最大半徑中位 1,245px／全場最大 **3,140px**。
   ⇒ 它一直在做實事，**不是冗餘**，但它的品質貢獻**從未被消融過**。
   ⚠⚠ 而且它會在 `add_new_gs` 之前把 `_max_radii2D` 清成 None
   ⇒ 讓 `cost_aware_densify` **永遠不觸發**（第 7 個靜默 no-op，§11.101，已修順序）。
3. **`results.txt` 會被 `main.py test` 從 `val/*` 覆寫成 `test/*`** ⇒ 一律從 tensorboard 讀。
4. **`test/` 可能有多組 ckpt 的圖**（earlyckpt 留四組）⇒ 用 `_final_test_dir()`，
   別用 `glob(test/*/*.png)`（曾汙染建築低頻 +22%）。
5. **批次刪／搬既有檔案**：條件要用「這次產生的子目錄名」正向指定；
   `rm -rf $VAR` 與 `find -newer` 曾兩次毀資料 ⇒ sed 複製腳本後跑 `tools/lint_task_scripts.py`。
6. **`results.txt` 不寫步數** ⇒ 半途 OOM 的跑次留下看似正常的分數
   ⇒ **比較前先跑 `tools/run_status.py`**。
7. 重編 rasterizer 要 `rm -rf build` + 強制 `CUDA_HOME` + `--force-reinstall`，**確認 .so mtime 變了**。
8. **⛔⛔ 絕不可用 `python open(p,"w")` 改「正在執行中」的 `.sh`**
   —— 那是**就地截斷、同 inode**，跑動中的 bash 用舊的位元組偏移繼續讀 ⇒ 讀到一行的中間。
   ✅ 用 `sed -i` 或「寫暫存檔再 `mv`」（**換 inode**，執行中的 fd 指向舊 inode，不受影響）。
   實測 inode：`sed -i` 71090->71091（換）／`python open(w)` 71091->71091（**不換**）。
   **2026-09-05 第二次踩**：改 `task_fpd_b12.sh` 時它還在跑 ⇒ bash 執行到
   `--model.initia|lize_from` 中間 ⇒ `lize_from: command not found`、rc=127，
   把「中止」偽裝成「腳本壞掉」。⚠ 我在同一個 session 裡才剛把這條寫進 §研究總覽 3924。
9. **⛔⛔ 手動啟動的跑次可能不是單變數 —— 比較前 `diff` 兩份 resolved config**
   （`outputs/<run>/blocks/block_*/lightning_logs/version_0/config.yaml`，**不是任務腳本**）。
   2026-09-11：`speed3_sfminit_b12` 與 `speed3_b12` 差**四項**，其中 `dynamic_strips`
   false→**true** ⇒ 逐條帶 SSIM 是實作自承的 boundary-window **近似**，
   而 `dynk_K.log` 顯示 **K>1 佔 77.7%** ⇒ 60k 分數作廢（也解釋了慢 24%、VRAM 低 0.79 GB）。
   ⚠ 附帶：`predict_num_strips` 的 budget 比硬體實際能吃的**低約 1.1 GB**
   （同參數下 depth-init 也被判 K=6，而它 K=1 實測峰值 4.87 GB 沒爆）⇒ K 是白付的。
   ⚠ `strip_safety` 是**預算乘數**（越大越寬鬆），不是折減係數。見 §13.7。
10. **⛔ `rend_alpha` 會把場外的巨大 floater 算成「有覆蓋」** ⇒ 量「那裡有沒有東西」
   一定要先濾掉 SfM 盒外的粒子（`tools/novel_view_coverage.py`）。
   同一個量測我做錯三次；第三次是**改了 `cam.T` 卻沒重算 `world_to_camera`/`full_projection`**
   ⇒ 五個視角 offset 覆蓋率完全相同（Camera 的矩陣是 `__post_init__` 算好的）。見 §13.8。

## 三個 2026-09-11 的定案（引用 opacity_reg / cap / VRAM 前先看）

```
1 收割期 opacity L1 **不可關**   oregstop_b12 三光度全輸（-7.1/-7.3/-5.7sd）紋理比 +6.7sd
                                 ＝第四次「虛假細節」簽名。關掉後同樣 2.34M 顆但
                                 **總 opacity 質量 +47%、o>=0.9 多 1.82 倍** ⇒ overdraw
                                 ⚠ 前提 §11.69「純損失」是用**死亡率**判的，判錯了對象
2 cap 只交付 90% 的真因           = trim 的 `prune_ratio: float = 0.1`（renderer:25），
                                 且 trim 閘在 `step <= densify_until_iter` ⇒ **收割期不跑**。
                                 **不是** opacity_reg 續壓（原歸因已更正）。
                                 ⇒ `prune_ratio` 是一個從沒當旋鈕試過的參數
3 VRAM 是**硬牆不是慢坡**        N 固定只改餘裕：每步 0.999~1.001x 平到撞牆，retries 全 0
                                 ⇒ 慢不是配置器在搬。純計算 ≈ 155ms/百萬顆 + **21ms** 固定，
                                 而全訓練固定項 179ms ⇒ 差額在 dataloader/全解析度 loss/
                                 Lightning，**不在 render**（要減固定成本往那側找）
                                 ⚠ `max_split_size_mb:128`（每支腳本都寫死）固定貴 **6.4%**
                                   換 +0.6GB 餘裕 —— 從沒對照過，裁決已排入佇列
```

## 破平衡公式（已 4/5 命中）

```
break-even interval = contribution_prune_interval × ln(1.05) / (−ln(1−prune_ratio)) = 231.5
interval > 231.5 → 顆數衰減；< → 成長
```
另：**顆數交付 = 0.9 × cap**（trim 每 500 步剪 10%、densify 每 150 步加 5%，
兩者同時在 densify_until 停 ⇒ 凍結在最後一次 trim 的輸出）。

## 常用指令

```bash
# 單塊訓練（現行最佳）
bash scripts/task_sched30.sh

# 多指標總表（含最差10%、建築低頻；從 tensorboard 讀，不看 results.txt）
python tools/audit_all.py

# 尾巴分析 / 結構誤差 / 分水面建築
python tools/tail_analysis.py <run>[:blk] ...
python tools/lowfreq_error.py <run>[:blk] ...
python tools/lowfreq_split.py <run> ...

# 比較分數前必跑（過濾半途死掉的跑次）
python tools/run_status.py --runs <name> ...

# 進度／死因
tail logs/quad_progress.log
```

## 隊列與待辦（2026-09-10）

```
跑中   speed3_sfminit_b12（54% @ 32k，ETA ~5h）—— 目前最強的活線
佇列   （空）
```
**待辦（依證據強度）**
```
1 ★★★★★ sfminit 完賽 -> tools/cmp_runs.py 對 speed3_b12 判定 -> **跑 b7**（鐵律）
2 ★★★★  SfM 點密度 x 糊掉遮罩的重疊率（純 CPU，便宜）
          —— 可能一次解釋掉整條糊掉調查（見本檔最上面的假說）
3 ★★★   cost_budget 重做：首次 OOM 於 step 22,352（N 衝到 3.10M）
          => B 的標定與 cap 安全閥都要收緊
4 ★★    blur_split_budget 從未跑過，且有**靜默稀釋**缺陷（trim 500 步 vs densify 150 步
          => 形狀對不上就跳過，三次只生效一次）=> 要用先補 padding
          （`cost_add_densify` 已用「補 ŝ=1」解決，可照抄）
5 ★     北極星：25 塊 / merge / 官方 741 幀 —— **我方從未量過泛化**，論文必須有
```
⚠ **不可當配方比較的跑次**（顆數/中止點不對齊）：
`speed3_ult_b12`（cap 1.7M + opacity_reg 0.007 ⇒ 只交付 0.77M = cap 的 45%）、
`speed3_aggressive`（OOM x2）、`cb_b12`（OOM）。

## 鐵律

只用當前資料（SfM 2026-05-29 重生）／**GT 修正 = 2026-08-12 10:08:37，之前開跑的一律作廢
且 bug 對內容重的塊傷害 2.13 倍**／outputs 全是這台 6GB 跑的、不准質疑硬體／
繁體中文＋ASCII 數學／指令用腳本或單行／大實驗前先 CPU 驗前提／拿檔案當基準前先查來源／
排程只用 `scripts/runner.sh`＋`queue.txt`，不准自開 watcher。
