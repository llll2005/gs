> ⛔ **已歸檔（2026-09-10）**：內容停在 2026-08-29（當時最佳＝`absgrad 1.0`，現為 `speed3`）。
> **待辦的單一來源改為 `研究總覽.md` §13.5 與 `_ctx.md` 的「隊列與待辦」。**
> 維持獨立待辦檔正是總覽開頭警告的「多份文件互相矛盾」的成因。

---

# 待辦（2026-08-29 重建；舊清單見 `archive/待辦與半成品清單_2026-08-06_已由新清單取代.md`）

**現行最佳＝`sched30` + `absgrad_densify 1.0`**（b12 26.43 / b7 25.28，兩塊四指標全勝，§11.47）

## 🔴 進行中（GPU 佇列）

| | 任務 | 決定什麼 |
|---|---|---|
| 1 | `cad_b12`（跑中）| 論文命題的另一半：往**便宜**的地方增生。`\|g\|` 那半已證實有效 |
| 2 | `trimsub`（8 分）| trim 用 24 台 vs 284 台排序是否一致 ⇒ **能否直接省 9%**（trim 佔 profile 80%） |

## 🟠 已量到前提、值得做

- **`absgrad_densify` 調強度**：目前 w=1.0，天花板 16.68× 還有空間（step 600 只吃到 17%）。
  ⚠ `vpc` 的教訓是強度過高會崩，要漸進（w=2 → 3）。
- **`absgrad` + `cost_aware` 合併**：`probs ∝ o·(1+w·g)/c^w'` ＝「每單位渲染成本的預期誤差下降」，
  **論文命題的完整形式**。等 `cad_b12` 出來再決定要不要合。
- **北極星（25 塊 + merge + 自跑 CityGS baseline）**：使用者已目視確認原版 web view 清楚而我方單塊糊，
  但**我方是單塊、它是合併場景，不可直接比** ⇒ 這是唯一能回答「差多少、差在哪」的實驗。
  ⚠ 前置阻塞（單塊配方）**已解除**：配方跨塊驗證通過了。

## 🟡 值得重驗（archive 裡、與當前方向一致、但依據已作廢）

- **K-strip / 動態 K**：三個否決依據全垮（跨 kernel confound／GT 錯位資料／顆數斜率低估 2.4 倍，§11.31）。
  τ 已重量：訓練後 `tau_mean` 漲 1.66×、`tau_MAX` 漲 5.4×，K=4 約買 1.96× 顆數 ≈ +0.54 dB。
  ⚠ **但 K 是「用時間換分數」**（7/24 自述一塊要跑兩天）⇒ 在單次 9h 的瓶頸解決前不投。
- **`min_opacity` 退火 / `screen_prune_emergency_px` / DBP**：實作了沒跑。
  ⚠ 三者都是**剪枝/回收**類，而 `vpc` 已證「破壞集合」無效 ⇒ 優先序低於「改變集合」類。
- **「trim kernel 半數顆數贏 gsplat」**：GT 錯位期的跨 kernel 比較，從未重驗。
  ⇒ 建議**不重跑**，報告裡只寫「沿用 CityGSV2 的 kernel」，不做優劣宣稱。

## ⛔ 已證死，不要重啟（每項都有量測，見 研究總覽 §7 與 §11.x）

`vpc_prune_frac`（搬移≠移除，−2.34 dB）／`scale_reg=0`（−18sd，塵埃是它做事的副產物）／
`notrim2`（b12 贏 b7 反轉）／`err_guided_densify`（**旗標是死碼**，且我方誤差分數天花板只有 1.22×）／
純時間壓縮 proxy（三時間尺度不可兼得）／`fused_ssim`（省 4.9% 但 SSIM 離群 −4.06sd）／
尾巴類介入（0/8：換 init、換配方、churn 對齊全部碰不到）

## 維護規則

- 新結論一律用 `tools/cmp_runs.py`（最後 4 點平均），**不要用終點值**（噪音差 2.9 倍）
- 單塊結果**必須跨塊驗證**才能進配方（`notrim2` 就是在這步翻的）
- 提機制解釋前先問「它預測什麼可量的量」並先去量（見記憶 `feedback_measure_before_explaining`）
