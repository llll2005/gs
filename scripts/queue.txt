# ════════════════════════════════════════════════════════════════════════════
#  執行佇列 — 最上面的先跑，跑完自動移除，後面往前遞補
#
#    · 一行 = 一個任務（長指令包成 scripts/*.sh；佇列行不要放 $(...) 或反斜線）
#    · `#` 開頭 = 註解；任務正上方註解區塊的第一行 = 台帳上的名稱
#    · 行首加 [cpu] = 不吃 GPU，不必排隊等卡（partition / depth-init / 分析）
#    · 跑中隨時可改（加/刪/調序都行），只有正在跑的那一行不能動
#
#  啟動： setsid nohup bash scripts/runner.sh > /dev/null 2>&1 < /dev/null &
#  停止： touch scripts/queue.stop        看狀況： tail -f logs/runner.log
# ════════════════════════════════════════════════════════════════════════════
#
#  【2026-08-01 重排】主線改為「3×3 全場景 → 合併 → 官方 741 幀 held-out 評測」。
#  原因：逐塊評測官方 test 本質上無效——單塊只擁有 1/9 的城市，而航拍視錐涵蓋範圍
#  遠大於此，畫面大半渲染成空白，PSNR 卡在 ~15 dB 與該塊好壞無關（已在 b7 上實測，
#  其自身訓練視角重建得很乾淨）。只有合併模型的數字能對外比較。
#  設定全部鎖定為 blk4 已跑的組態：cap 3M / opacity_reg 0 / screen_size_prune_px 300。
#  每塊約 10.5 小時；先跑十字（1,3,5,7）與既有的 4 湊成連通區塊，可提早約兩天先合併看結果。
# ════════════════════════════════════════════════════════════════════════════

# 3×3 全場景 ① blk1（上中）— 與既有 blk4 相鄰，優先湊出連通區

# 平原期補測（純推論，14 個既有 ckpt，約 20 分）— 把「沒測過」變成有結果
#   07-19~28 那批被記成「無效」，但同期 OOM 死掉的跑次 LPIPS(0.729) 反而優於跑完的(0.737)，
#   代表該區間指標沒有鑑別力。改用內容分組(水面/建築)+紋理比，並把死掉的跑次當已知壞模型校驗尺。

# 訊號天花板量測（純推論，3 個既有 ckpt，約 10 分）— 決定 DBP/DGD 該不該復活
#   07-29 以「天花板僅 1.2×」否決誤差導向 densify，但那量的是【光度】誤差、且未分內容組。
#   DGD 用的是 SSIM 梯度（L1 對「糊」不敏感是該論文的核心論點），而 b12 一半是水會把集中度
#   統計往 1.0 稀釋。這裡把三種缺口（光度/結構/紋理）在水面組與建築組分開量。

# 3×3 全場景 ② blk3（中左）

# 3×3 全場景 ③ blk5（中右）— 既有的 b3x3_blk5_reg000_depth_modify 是不同組態(cap1.7M/prune -1)，不可混用

# 3×3 全場景 ④ blk7（下中）— 跑完即成十字＋中心的五塊連通區

# 中途合併與官方 held-out 評測（五塊連通區）— 第一個能對外比較的數字

# 3×3 全場景 ⑤ blk0（左上角）

# 3×3 全場景 ⑥ blk2（右上角）

# 中途合併與官方 held-out 評測（blk2 完成後，共 7 塊）— 第一個能對外比較的數字
#   2026-08-03：merge_citygs_ckpts.py 的 KeyError(shs_rest 無 optimizer state) 已修並在 CPU 驗證通過，
#   6 塊合併出 4.55M 顆、結構檢查全過。此處提早取數，不必等 blk6/blk8。
#   ⚠ 缺 blk6/blk8 兩角，涵蓋該區域的測試視角會渲染成空 → 數字偏低，看逐來源區塊的拆分表。

# 3×3 全場景 ⑦ blk6（左下角）

# 3×3 全場景 ⑧ blk8（右下角）

# 完整九塊合併與官方 held-out 評測

# ── 以下暫停，等有了 held-out 尺再判斷 ─────────────────────────────────────
# coarse-init 相容性檢查（200 步）— 確認載入 sh0 coarse 後 SB 色彩沒被降級
#暫停# bash scripts/run_coarse_init_check.sh

# coarse-init 全長臂（reg=0）— 分辨 floater 是 depth-init 的起點性質還是 opacity_reg
#暫停# bash scripts/run_coarse_init_arm.sh

# opacity_reg 掃描（0 / 0.0005 / 0.002 / 0.007）— 已跑 0、0.002、0.007，全是 val⊂train 的數字。
#   val 看不見 floater，所以這個掃描要等合併模型的 held-out 尺出來才有辦法正確判讀。

# 官方原版全域 coarse（診斷用，30k 步，估 4~6h）— 唯一還站著的差異：全域聯合優化
#   我方逐塊模型的渲染表面恆在真值 0.75 倍深度處（屋頂高度的毯子），step 499 就成形。
#   其他解釋全部已被量測排除。此臂測「全域聯合優化會不會避開毯子」。
#   ⚠ 不是 27.23 的重現，數字不可拿去比論文。停在 coarse，不跑會 OOM 的微調階段。

# 官方原版全域 coarse（第二次；首次 rc=137 是 RAM OOM，非 CUDA）— 已加 train_max_num_images_to_cache 512

# 官方原版全域 coarse（第三次）— 首次 rc=137 RAM OOM、二次 CUDA OOM(起始 SfM 雲 3.83M @ sh2)
#   本次唯一新增偏離：down_sample_factor 1.2 -> 2（資料端，不碰機制；理由見 config 註解）

# 官方原版全域 coarse（第四次）— 前三次：RAM OOM / CUDA OOM(SfM 雲 3.83M@sh2) / 深度圖與影像解析度不合
#   本次修 citygsv2_metrics 的深度 resize（沿用 mcmc_inverse_depth_metrics 既有慣例，形狀相同時 no-op）

# 官方微調 4x4 blk5（單塊試跑）— 從官方 coarse 出發，量微調後的深度偏差
#   coarse 自己的深度量測回答不了問題（它是刻意粗略的代理，50 萬顆蓋整座城、視角間變異極大）。
#   要看的是「從 coarse 微調出來的區塊模型」，那才和我方逐塊模型可比（對照 slope 0.713）。
#   ⚠ 梯度 densify 無 cap，先跑一塊觀察再決定其餘 15 塊。

# 24k 激進配方 5x5 單塊試跑（b12）— cap 3M，找 VRAM 天花板在哪
#   我方 5x5 幾何是對的（b12 60k cap2M: slope 1.082 / corr 0.903），毯子是 3x3 佈局造成的。
#   這一臂測「壓到 24k + cap 3M 還撐不撐得住」；撐得住的話 25 塊全場景從 5 天降到 2 天。

# 24k 激進配方 b12 的評測 — val 指標 + 內容分組(水面/建築)+紋理比
#   對照同塊 60k cap2M：val 23.39/0.667/0.491，slope 1.082/corr 0.903
#暫停# bash scripts/run_24k_aggr_eval.sh 12

# 24k 激進配方 5x5 b12（使用者調參版）— cap 3M / sb_number 3 / means_lr 3.1x / opacity_reg 0.002
#   早停判準：step1500 不透明度中位>0.5、step5680 val PSNR>=21、step6000 深度 slope~1.0
#   ⚠ 已知風險：5x5@1.80M 曾峰值 5.63/6.1G，cap 3M 線性外推約 9.4G，可能 OOM

# 24k v2（b12）— 修顆數收支：prune_ratio 0.05 / contribution_prune_interval 850 → 平衡點 809
#   v1 用預設 0.1/500 → 平衡點 231，而 interval 350 在其上 → N 從 338k 萎縮到 77.6k。
#   同時修針狀物：means_lr_init 0.0002→0.0001、scale_reg 0.006→0.008（v1 長寬比 P50 2.19 vs 參照 1.06）
#   推估 N 會長到約 1.14M。對照：v1 終點 22.07/0.604/0.695 @77.6k；參照 23.39/0.667/0.491 @1.80M

# 24k v2 評測 — val + 內容分組（水面/建築）+ 紋理比，與 v1 和參照同表比較
#暫停# bash scripts/run_24k_v2_eval.sh 12

# 24k v3（b12）— densification_interval 350→100，目標 step 9000 衝到 cap 3M、15000 起純精修
#   收支用「有效」剪除率 17%（opacity_reg → o<1/255 → EXACT_SUPPORT 排除 → 零貢獻 → 被剪）
#   ⚠ churn 觀察點：step 5,680 的 val 若 <20 就是 interval 太短（首次用 60 拿 19.51，v1 用 350 拿 20.64）

# 24k v3 評測

# ════════════════════════════════════════════════════════════════════════════
#  【2026-08-09】blur-split 系列。噪音底線已量到（blursplit_b12 ＝ cap4m 的近似複本）：
#  PSNR 0.053 / SSIM 0.0024 / LPIPS 0.0022 / 紋理比 0.0037。以下都對照 cap4m 24.307。
#  跑中的 trimstop_b12 由舊 driver 起的，runner 會等它讓出卡再接手。
# ════════════════════════════════════════════════════════════════════════════

# blur budget 評測（渲染圖 + test_speed）

# floater 是否落在低紋理區（梯度飢餓假說的前提驗證，CPU 不佔卡）

# blur budget + 長軸散開 — 挑對宿主之後，子代不再全疊在原點

# blur budget + LR 下限×10 — 兩機制買的東西不重疊（前者 LPIPS 5.9x 噪音、後者 PSNR 3x 噪音）

# ★視角相依：SB 換回 SH3（等 VRAM 對齊 cap 2.6M）— 低頻誤差 45% 是對比壓縮到 66%，
# 且與顆數無關 ⇒ 疑為跨視角平均；同時檢驗水面鬼影是否為漫射模型硬湊反射所致

# 快長 + 長精修 — 12k 步到 cap、35k 步精修，顆數不減（取代 densifystop：那臂的顆數混淆
# 約 0.2 dB，和待測效應同量級，正負都解釋得通；且就算答案是 churn 有害也沒有可執行動作）

# SH3 評測（渲染圖）— 水面鬼影是否消失、低頻對比壓縮是否改善，這兩個才是假說的直接判準

# ★SH3 + blur split，cap 3.4M（SH3 位元上限 3.13M）— SH3 原本 cap-bound 在 2.34M

# [cpu 不佔卡] reg=0 的渲染圖 — 對比壓縮對顆數/init/LR/blur/顏色模型全免疫，剩 opacity_reg 沒驗過

# 同上，補齊 opacity_reg 的三點單變數掃描（同 config 同 cap，只差 reg）— 幾分鐘就有答案

# SH3 + reg 0.008（使用者指定）— 若上面三點顯示 reg 不影響對比壓縮，可直接刪掉這行省 11 小時

# ★消融：拿掉法向一致先驗（lambda_normal 0）— 從未消融過，且在所有跑次裡恆為 0.0125，
# 正是對比壓縮「對一切免疫」的簽名；而它是平滑先驗，我們的病症正是過度平滑

# ★消融：拿掉深度損失 — 最後一個 DA2 依賴；深度監督已量到榨乾（偽深度誤差 174× 表面解析度、
# 62.8% 粒子已落在其誤差內）。若可砍，整條 estimate_dataset_depths 流程都能刪

# 補上最後兩個的渲染圖（test 只要 90 秒，但沒圖就無法目視判斷幾何是否退化）

# opacity_reg 三點單變數掃描（同 config 同 cap，已訓練好，只差渲染圖）— 各約 90 秒

# SH3 + reg 0.008（使用者指定）→ 隨即出圖。若上面掃描顯示 reg 不影響對比壓縮，可刪這兩行省 11h
# ⛔ 已刪除：SH3+reg0.008。目的(a)驗 reg 是否造成對比壓縮 —— 三點掃描 90 秒就答完了，
#    答案是「不是，而且方向相反」(reg0 壓縮最嚴重 59.3%，reg0.002 最輕 64.7%)；
#    目的(b)為 SH3 重調 reg —— SB 上 reg0.007 比 0.002 差 0.53 dB，期望值低。

# ★消融：拿掉法向一致先驗 → 隨即出圖（它是 2DGS 自己的表面先驗，幾何退化只看得出來、量不出來）
# ★★★ 北極星：b3x3_full 九塊已全部訓練完成，卻從未合併、從未在官方 741 幀評測。
# 這是唯一能與論文 27.23 直接比較的量測，約一小時。在它產出之前，所有關於差距的說法
# （包括我說的「26 dB 需要 100 倍顆數」）都是在錯的尺上外推。

# SH3 + blur split 重跑，cap 改為 2.6M（原本 3.4M 在 3.02M OOM）。與 sh3_viewdep 同 cap
# ⇒ 真正的單變數對照（後者實得 2,339,999 顆 / 24.672 / 0.7025 / 0.4500 / 0.3921）

# ★★★ 北極星：b3x3_full 合併 + 官方 741 幀（用既有腳本，偏移 -1 早已在訓練視角上以 7dB 確立）

# ★ 官方 741 幀重測，offset 0（姿態比對驗出：相機 N ↔ 影像 N+1，741 台殘差 0.000000）


# ★消融：拿掉深度損失 → 隨即出圖（影響在早期，看 step 5,680 的 val 不要只看終點）

# ⚠ 2026-08-12：dataparser 的 zfill 對應 bug 已修（相機 N ↔ 影像 N+1，先前差一格）。
# 當前資料上的所有既有訓練結果作廢，以下重跑。先跑最佳組態確認修好之後品質是否躍升。

# 修正後重跑兩個消融（先前因缺反斜線 rc=127 空跑，已修）

# ════════════════════════════════════════════════════════════════════════════
#  【2026-08-12 重建】dataparser 的檔名對應 bug 已修（相機 N ↔ 影像 N+1，先前 zfill 差一格）
#  ⇒ 當前資料上的所有既有訓練結果作廢。先重跑最佳組態確認品質躍升，再跑消融。
# ════════════════════════════════════════════════════════════════════════════

# 最佳組態重跑（SH3 / cap 2.6M / reg 0.002）— 對照作廢的 24.672，看修正後躍升多少

# 消融：拿掉法向一致先驗
# ★ SB 對照（位元對齊 cap 3.6M）— 修正後重測「SH3 是否仍勝過 SB」。
# 理由：我對 SH3 優勢的解釋是「跨視角平均」，而用鄰幀 GT 訓練正是在人為製造跨視角不一致
# ⇒ SH3 的優勢可能有一部分只是在補償這個 bug。機制結論要重新建立在修正後的資料上。
# ★ 顆數衰減組（24k 排程，約 1.5h）— 修正後「少而準」是否成立。
# 舊版 77,089 顆 22.022 vs 3.6M 的 24.307（23 倍顆數只差 1.37 dB），但建築紋理比只有 0.145
# ⇒ 是「少而糊」。而錯位 GT ≡ 模糊目標，小模型最沒有餘裕吸收 ⇒ 這一臂受害最重、修正後增益應最大。



# 消融：拿掉深度損失（最後一個 DA2 依賴）

# ════════════════════════════════════════════════════════════════════════════
#  累積式測試：每一跑 = 目前最佳 + 一個機制。目前最佳 = sh3_nonormal_b12
#  （SH3 / cap 2.6M / opacity_reg 0.002 / lambda_normal 0）= 25.828 / 0.7632 / 0.3734 / 0.4854
#  這樣每個量測都是在「實際會出貨的組態」上做的 —— 本專案已兩次量到疊加互相抵消
#  （blur split x LR 下限、blur split x SH3），對舊基準量到的增益不保證轉移。
#
#  ⏸ 4x4 全場景（16 塊 / 7 天）等配方定調後再排：
#     bash scripts/task_4x4_train.sh  然後  bash scripts/task_4x4_merge_eval.sh
# ════════════════════════════════════════════════════════════════════════════

# 最佳 + lambda_dssim 0.5（判準看 LPIPS 與紋理比，不看 PSNR）

# ★ 兩個幾何先驗一起砍（lambda_normal 0 + depth loss 0）—— 各自對基準都是正向
# （+0.393 / +0.240）但都在約束幾何，增益很可能重疊，不能相加。這一跑量的是「兩者一起」。
# 若成立，整條 DA2 流程（5,621 張影像的深度估計 + 校準 + 法向 + 體素去重）都能刪掉。

# ★ SSIM 權重的第三點（0.2 → 0.5 → 0.8）。0.5 那跑順便讓 floater 降 20%，而它沒有任何
# floater 機制 —— 若成立，機制是「SSIM 是窗口比較，懲罰量正比於螢幕足跡」＝隱式的 c_i 定價，
# ★ 副產物審計（幾分鐘，但要獨佔 GPU）：現行配方把 lambda_normal 設 0、lambda_dist 預設 0，
# 於是 depth_to_normal / rend_dist / view_normal 每步都算卻沒人用（view_normal 更是從來沒人用）。
# A 量成本、B 量 rend_dist 是不是新訊號（它逐像素量「視線上樣本多分散」＝ overdraw，
# 但若與 alpha/覆蓋高度相關就不是新資訊）。

# 那正是我們在密度控制裡顯式做而被退化定理否證的東西。兩點分不出機制與巧合，第三點可以。

# ★ 顯式的粗尺度 L1（1/8 解析度）—— CPU 實測發現大足跡誤差同時落在 L1 與 SSIM 的盲區
# ★★ 兩個先驗全砍 + dssim 0.5 —— 至今最強的兩個改動從未合過。
# noprior 26.083 是第一次越過 26，但 floater 1.726%（dssim05 的 1.8 倍）；
# 而提高 SSIM 權重是目前唯一被觀察到會【降低】floater 的改動（1.193% → 0.958%），正好對症。

# ★★ 每位元組買到多少品質：SB 加到 4 個 lobe（F=37）+ 位元對齊 cap 3.5M。
# 診斷指出瓶頸是「每單位面積的粒子數」，而 cap 被 VRAM 鎖死、切細塊要線性增加時間
# ⇒ 唯一免費的槓桿是【每顆的位元組】。SB 每個 lobe 只要 6 float，SH2→SH3 卻要 21。
# 且我們場景的視角相依內容是水面鏡反射＝角度上稀疏的銳利 lobe，SB 天生擅長、SH 要用一堆低階基底疊。
# ⇒ SB-2 輸 SH3 可能是「兩個 lobe 不夠」而非表徵較差；lobe 數從沒掃過（2 是 DBS 預設）。

# ★ lobe 軸的另一端：16 個 lobe（F=109，2.21M 顆，比 SH3 的 2.34M 還少）。
# 與 sb4 一起跑才能分開兩件事：sb4 贏而 sb16 輸 ⇒ 顆數主導，別再加 lobe；
# sb16 贏 ⇒ 角度表達力勝過顆數（它顆數更少還贏），那才值得去做逐粒子自適應 lobe。
# 免費後續（不用 GPU）：從 ckpt 讀 lobe 振幅分布，看「一顆粒子實際用幾個 lobe」——
# 若多數只用一個、只有水面/玻璃用多個，那就量到了自適應能省多少，再決定要不要寫 CUDA。

# （等誤差量下 SSIM/L1 懲罰比從 r=2 的 15.1 單調降到 r=64 的 1.14），而低頻仍佔 37% 誤差能量。

# ★★ 配方轉移測試（使用者提議）：現行最佳 noprior 換到 block_7，唯一變數是 block_id。
# b12 在 25 塊裡內容密度**排第 13、低於中位數**（GT 梯度 0.01547，半面水），b7 是**最重的一塊**
# （0.02189 = 1.42x），且是官方 baseline 重跑用的塊。在繼續拿 b12 調參之前先確認結論轉不轉得動。
# 判讀 vs b12 的 26.083：落 25~26 = 會轉移；<24 = 過擬合到低內容；OOM = VRAM 餘裕是 b12 特有的
# （這本身就是全場景 25 塊會一路踩的雷）。順便給合併分數一個有內容加權的推估底。

# ⚠⚠ 重跑 noprior：2026-08-17 我用 sed 產生 task_b7.sh 時漏改 `rm -rf outputs/noprior_b12`，
# b7 一啟動就把它整個刪光（ckpt/test圖/tensorboard 全沒了）。數字都在紀錄裡，但模型沒了 =>
# 後續所有結構分析（低頻誤差、分區域、對照圖）都缺這個參照，必須重建。
# ★副產物：它同時是**修正後從未量過的種子噪音**——把重跑結果對紀錄的 26.083 / 0.7693 /
# 補跑：上一輪漏給 block 參數（task_test.sh 預設 block 12，b7 沒有 block_12）而秒失敗。

#   0.3611 / 0.5005，差多少就是噪音底，全專案所有顯著性宣稱都靠它。

# ★★ 排程壓縮 2x2（b12 x b7）。densify_until 42k->30k 不是「多給步數」而是收斂預算 x3.02，
# 且 30k/60k=50% 正好回到上游 vanilla 的比例（15k/30k）——我們 2026-06-25 漂到 70%，
# 而那次調參在 GT 修正之前、依據已作廢。顆數 26,879(b7)/28,399(b12) 撞 cap => 30k 是最早切點。
# **b7 先跑**：它到 60,000 仍在爬(+0.107)，是有餘裕卻預算不夠的那個，機制若成立效應最大；
# b12 已在 56.8k 飽和，預期只是打平。兩個一起才能把「內容決定步數需求」從 n=2 變成 2x2。

# blur-split 預算 0.3：目前唯一「改變粒子集合」而非「改損失權重」的臂。低頻結構缺口已證實
# 不是損失工程能補的（coarsel1 佔目標 5.3% 只推動 1.5%），所以改變集合的手段相對重要性上升。

# 修正後的顆數標度律：全專案唯一同基底顆數對照（cap 1.3M vs 2.6M，唯一變數 cap_max）。

# ★ densify_until 掃描（36k / 24k）：sched30 是唯一有效的東西，但三個機制故事全被推翻。
# 單調 => 晚期 densify 純有害，整條 densify 排程要重設計；30k 附近有極值 => 與族群建完時點有關。
# ★★ 顆數線重開：halfcap 量到修正後斜率 **+0.541 dB/加倍**（bug 期估的 +0.229 的 2.4 倍，
# 而 +0.229 正是當初「count 不是主槓桿」的依據）。cap 2.6M->3.5M 預期 +0.232，與 sched30 同級。
# 疊在現行最佳 sched30 上，唯一變數 cap。⚠ 先前算的「SH3 天花板 2.9M」站不住（VRAM 記的是
# reserved 峰值，1.17M 用 5.70G 比 3.15M 的 5.53G 還多，對容量沒有鑑別力）=> 只能實測。

# ★ cap35 OOM 於 N=3.28M（碎片化，非容量；expandable_segments 需 torch>=2.1，本環境 2.0.1 不可用）
# => SH3 實測天花板約 3.28M，我們一直跑 2.34M。cap 3.0M 峰值離天花板有 8.5% 邊際，
# 預期 +0.112 dB（3.4x 噪音底），疊在 sched30 上目標約 26.49。

# 對照 noprior 26.0504 / sched30 26.3771，噪音底 PSNR 0.0325。判讀同時看 PSNR 與建築低頻。

# ★★ 使用者目視發現的疊影（best_b7）。四個假設已 CPU 否證（收斂/多層/供給/配對），
# 站得住的是**粒子相對尺度**：b7 2.06 vs b12 1.47（長軸/表面取樣間距，已正規化）
# => 粒子橫跨深度不連續處，架成半透明的膜。本臂壓 scale_reg 0.007->0.02。
# ⚠ 主判準是相對尺度與**目視**，PSNR 只當護欄 —— 這問題是眼睛找到的，數字全沒抓到。

# ★ 最便宜的診斷（~3 分鐘 GPU）：尾巴視角是「從頭就爛」還是「曾經好過後來壞掉」。
# 修好最差 25% 值 +1.551 dB（sched30 的 4.7 倍），而六個視角層級假設全否證，
# 且訓練損失本來就對誤差最大的視角給最大梯度卻修不好 => 不是分配問題。
# 從頭就爛 -> 初始化局部極小 => 下一步換 init（SfM-init 零成本）
# 曾經好過 -> densify/trim 弄壞的 => 下一步保護那些區域

# ★★ 多指標排名後的合成配方：cap30（平均名次第一）+ blurcum 的 blur_split_budget 0.3（最差10% 第一）。
# 五個指標五個不同第一名、Pareto 前緣四個 => 單一指標宣布勝負是錯的（研究總覽 §12.30）。
# 重跑：第一版腳本用 `find -newer` 搬圖，把 sched30_b12 原本的 60k test 圖一起搬走並同名覆蓋
# （毀掉 36 張）。已改成按 main.py 自己的子目錄名搬。這次會補齊 15k/30k/42k/60k 四組。

# ⚠ 疊加不可假設可加（本專案已量到兩對互相抵消）。開獎跑 `python tools/audit_all.py` 多指標比。

# ★★ b7 的顆數天花板：scale_reg 證明尺度壓不下去（2.9 倍正則只換 4%），是重建損失頂回來
# => 粒子需要那麼大才能覆蓋表面。b7 的 SfM 取樣間距與 b12 相近但粒子大 1.4 倍
# ★★★ 打開一個從沒跑過的機制：`screen_size_prune_px 300` 在每個跑次都是死參數
# （_max_radii2D 只在 screen_prune_emergency_px>0 時被填，而它預設 -1 且從沒設過；
#  runner.log 裡 "screen-prune" 出現 0 次）。原始碼註解自己就寫了要兩個一起設。
# 它正是專門剪超大螢幕足跡粒子的機制，而疊影的成因量到就是粒子相對尺度過大 => 直接對症。
# ★★★ 尾巴診斷已開獎：45,000 步把平均改善 39%，卻只把最差視角改善 4.5%（1729 在 30k 後完全不動）
# => 尾巴不是被 densify/trim 弄壞的，是**從頭就沒成功過** => 事前判準指向換初始化。
# SfM-init 零成本（`--model.initialize_from null`），修正後從未測過。
# ⚠ 判讀看**最差 10%**（+1.55 dB 在那裡），不是平均；且必須看圖。

# ⚠ 必須看圖 + 多指標（tools/audit_all.py），不可只看 PSNR。

# => 空拍 SfM 在立面稀疏、真實表面積被低估 => b7 可能供給不足。b7 的 cap 從沒測過。
bash scripts/task_cap30_b7.sh
bash scripts/task_test.sh cap30_b7 7
