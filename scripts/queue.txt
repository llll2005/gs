# ★★★ 起始 trim 鎖死了錯的前層（研究總覽 §11.2）—— 這是尾巴那 +1.55 dB 的第一個真正入口。
# b12 實測：depth_init 的 1,211,537 顆裡**只有 157 顆真的在視錐外**，卻被 step 1 的起始 trim
# 砍掉 873,504 顆（72.1%）=> 砍的是「看得見但被前層遮住」的點，含正確的表面。
# 成因：depth_init 給每一顆都設 opacity 0.99，而殼比表面厚 20 倍 => 前層吸光 transmittance。
#
# 本臂攻根因：把 init 的 opacity 改成 0.5（只改 opacity，位置/尺度/法向/顏色不動）。
# 五層深處的 transmittance 從 1.0e-10 變成 3.1e-2 => 後層拿得到梯度。
# ⚠ 判讀看**最差 10%**（tools/tail_analysis.py）與**看圖**，不是只看平均。
bash scripts/task_op05.sh
bash scripts/task_test.sh op05_b12

# 歸因對照：只關掉 step 1 那一刀，opacity 維持 0.99、保留全部 1.21M 顆。
# op05 有效而本臂中性 => 問題是**遮擋**（後層拿不到梯度），不是那一刀本身。
# 預期偏中性，而那個中性本身就是資訊。
bash scripts/task_notrim.sh
bash scripts/task_test.sh notrim_b12
